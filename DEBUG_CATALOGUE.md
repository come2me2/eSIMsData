# 🔍 Отладка проблемы с пустым каталогом

## Проблема
Каталог возвращает 0 стран и 0 тарифов, хотя API должен возвращать данные.

## Шаги отладки

### 1. Проверьте сырой ответ от API

Откройте в браузере или через curl:
```
GET /api/esimgo/catalogue-debug
```

Этот endpoint покажет:
- Структуру ответа от API
- Тип данных (массив или объект)
- Ключи в ответе
- Пример первого элемента
- Полный ответ (первые 2000 символов)

### 2. Проверьте логи в Vercel

1. Зайдите в Vercel Dashboard
2. Выберите проект
3. Перейдите в **Functions** → **Logs**
4. Найдите логи с префиксом `Catalogue received` или `processCatalogue`

### 3. Проверьте API ключ и доступ

Убедитесь, что:
- `ESIMGO_API_KEY` установлен в Vercel
- API ключ активен и имеет доступ к каталогу
- Организация имеет доступные Bundle в инвентаре

### 4. Проверьте прямой запрос к API

```bash
curl -H "X-API-Key: YOUR_API_KEY" \
  "https://api.esim-go.com/v2.4/catalogue?perPage=10"
```

Это покажет, что именно возвращает API напрямую.

## Возможные причины

### 1. API возвращает пустой массив
**Причина:** В инвентаре организации нет доступных Bundle
**Решение:** Проверьте инвентарь в eSIM Go Portal

### 2. Неправильная структура ответа
**Причина:** API возвращает данные в другой структуре
**Решение:** Используйте `/api/esimgo/catalogue-debug` чтобы увидеть структуру

### 3. Проблема с пагинацией
**Причина:** Нужно указать правильные параметры пагинации
**Решение:** Попробуйте запрос с параметрами `page=1&perPage=50`

### 4. Фильтры не работают
**Причина:** Параметры фильтрации неверны
**Решение:** Попробуйте запрос без фильтров сначала

## Что проверить в ответе от `/catalogue-debug`

1. **isArray** - должен быть `true` если API возвращает массив
2. **bundlesCount** - количество Bundle в ответе
3. **sampleItem** - пример Bundle, проверьте структуру
4. **firstItemKeys** - ключи в Bundle объекте

## Следующие шаги

После получения информации из `/catalogue-debug`:
1. Проверьте структуру ответа
2. Обновите `processCatalogue` если структура отличается
3. Проверьте, что Bundle имеют поле `countries` с данными

