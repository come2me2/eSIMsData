# 🚀 Развертывание на VPS Contabo

Проект готов к развертыванию на VPS Contabo с поддержкой Node.js API.

## 📋 Что было сделано:

1. ✅ Создан Express сервер (`server.js`) для API endpoints
2. ✅ Настроен PM2 для управления процессом (`ecosystem.config.js`)
3. ✅ Обновлена конфигурация Nginx для проксирования API
4. ✅ Создан `package.json` с зависимостями
5. ✅ Добавлены инструкции по развертыванию

## 🎯 Быстрый старт:

Следуйте инструкциям в файле **`DEPLOY_CONTABO.md`** для быстрого развертывания.

Или используйте подробную инструкцию в **`SETUP_CONTABO.md`**.

## 📁 Структура файлов для VPS:

```
├── server.js              # Express сервер
├── package.json           # Зависимости Node.js
├── ecosystem.config.js    # PM2 конфигурация
├── nginx.conf             # Конфигурация Nginx
├── .env.example           # Шаблон переменных окружения
├── update.sh              # Скрипт для обновления
├── DEPLOY_CONTABO.md      # Быстрая инструкция
└── SETUP_CONTABO.md       # Подробная инструкция
```

## 🔑 Необходимые переменные окружения:

Создайте файл `.env` на основе `.env.example`:

```bash
ESIMGO_API_KEY=your_api_key_here
PORT=3000
NODE_ENV=production
```

## 🛠️ Основные команды:

```bash
# Установка зависимостей
npm install --production

# Запуск через PM2
pm2 start ecosystem.config.js

# Просмотр логов
pm2 logs esimsdata

# Обновление проекта
./update.sh
```

## 📝 Примечания:

- Node.js версия 18.x или выше
- PM2 управляет процессом и автозапуском
- Nginx проксирует запросы на Node.js (порт 3000)
- Статические файлы отдаются из `/public`
- API endpoints доступны через `/api/*`

## 🔗 Полезные ссылки:

- [PM2 документация](https://pm2.keymetrics.io/docs/usage/quick-start/)
- [Nginx документация](https://nginx.org/en/docs/)
- [Express документация](https://expressjs.com/)

