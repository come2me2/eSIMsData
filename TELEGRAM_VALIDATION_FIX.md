# 🔧 Исправление проблемы валидации Telegram

## Проблема
```
Hash mismatch - data may be tampered
```

## Возможные причины

### 1. BOT_TOKEN не установлен в Vercel
**Решение:**
1. Зайдите в Vercel Dashboard
2. Settings → Environment Variables
3. Добавьте переменную:
   ```
   Name: BOT_TOKEN
   Value: ваш_токен_бота_от_BotFather
   Environment: Production, Preview, Development
   ```
4. Передеплойте проект

### 2. Неправильный формат initData
**Проблема:** initData может быть уже декодирован или изменен

**Проверка:**
- initData должен быть строкой в формате URL query string
- Должен содержать параметр `hash`
- Не должен быть декодирован (должен быть в исходном виде)

### 3. Проблема с кодировкой
**Решение:** Убедитесь, что initData передается как есть, без изменений

## 🔍 Отладка

### Шаг 1: Проверьте BOT_TOKEN
```bash
# В Vercel Dashboard → Functions → Logs
# Найдите логи с "Hash validation attempt"
# Проверьте hasBotToken: true
```

### Шаг 2: Проверьте initData
В консоли браузера (в Telegram Web App):
```javascript
// Проверьте, что initData существует
console.log('initData:', window.Telegram.WebApp.initData);

// Проверьте, что он содержит hash
const params = new URLSearchParams(window.Telegram.WebApp.initData);
console.log('Has hash:', params.has('hash'));
console.log('Hash value:', params.get('hash'));
```

### Шаг 3: Проверьте логи сервера
В Vercel Dashboard → Functions → Logs найдите:
- `Hash validation attempt` - показывает, что валидация началась
- `Hash comparison` - показывает сравнение хешей
- `Telegram validation` - финальный результат

## ✅ Правильная настройка

### 1. Получите токен бота
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Выберите "API Token"
5. Скопируйте токен (формат: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2. Установите в Vercel
```
Name: BOT_TOKEN
Value: 123456789:ABCdefGHIjklMNOpqrsTUVwxyz
```

### 3. Проверьте формат initData
initData должен быть в формате:
```
user=%7B%22id%22%3A123456789%2C...%7D&hash=abc123...
```

**НЕ должен быть:**
- Декодированным JSON
- Измененным или модифицированным
- Пустым или undefined

## 🧪 Тестирование

### Тест 1: Проверка BOT_TOKEN
```bash
# В Vercel Dashboard проверьте логи
# Должно быть: hasBotToken: true
```

### Тест 2: Проверка initData
В консоли браузера:
```javascript
const auth = window.telegramAuth;
console.log('initData:', auth.getInitData());
console.log('User:', auth.getUser());
```

### Тест 3: Ручная валидация
```bash
curl -X POST https://your-project.vercel.app/api/validate-telegram \
  -H "Content-Type: application/json" \
  -d '{"initData":"user=...&hash=..."}'
```

## ⚠️ Важные моменты

1. **BOT_TOKEN должен быть правильным** - токен от BotFather для вашего бота
2. **initData не должен изменяться** - передавайте как есть из `window.Telegram.WebApp.initData`
3. **Проверьте свежесть данных** - auth_date не должен быть старше 24 часов
4. **Используйте HTTPS** - для безопасности

## 🔄 Альтернатива: Отключить валидацию (только для разработки)

Если нужно временно отключить валидацию для тестирования:

```javascript
// В checkout.js временно закомментируйте валидацию
// const validation = await auth.validateOnServer('/api/validate-telegram');
// if (!validation.valid) {
//     throw new Error(validation.error || 'Validation failed');
// }
```

**⚠️ ВНИМАНИЕ:** Не используйте это в продакшене! Валидация критична для безопасности.

