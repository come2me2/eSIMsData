# План оптимизации кэширования для мгновенной загрузки

## 🎯 Цель
Пользователи должны **моментально** видеть актуальные данные из серверного кэша без ожидания загрузки из API.

## 📊 Текущая проблема

1. **Агрессивная очистка** при каждой загрузке удаляет весь localStorage
2. Пользователи всегда ждут загрузки из API (медленно)
3. Клиентский кэш не используется эффективно

## ✅ Решение: Статические JSON файлы + Memory Cache

### Архитектура

```
Пользователь открывает страницу
    ↓
1. Memory Cache (сессия) → HIT? → Мгновенно ✅
    ↓ Нет
2. Static JSON File (/data/*.json) → HIT? → Мгновенно ✅ (nginx отдает напрямую)
    ↓ Нет
3. API Request → Получение данных → Сохранение в Memory Cache → Отображение
    ↓
4. Фоновое обновление статических файлов (если нужно)
```

### Изменения

#### 1. Убрать агрессивную очистку localStorage
- **Удалить**: код, который очищает все записи при загрузке
- **Оставить**: только проверку версии кэша (удалять только старые версии)

#### 2. Отключить localStorage для планов
- **Оставить**: только Memory Cache (в рамках сессии)
- **Убрать**: сохранение в localStorage для планов
- **Причина**: Статические файлы уже кэшируются браузером через nginx

#### 3. Приоритет статических файлов
- **Для планов**: Static JSON → API (fallback)
- **Для стран**: Static JSON → API (fallback)
- **Memory Cache**: только для текущей сессии

#### 4. Автоматическое обновление статических файлов
- При обновлении серверного кэша через `/api/cache/prefill`
- Автоматически генерировать статические JSON файлы
- Это гарантирует актуальность данных

## 🔄 Что будет, если отключить клиентский кэш?

### Вариант 1: Полностью отключить (localStorage + Memory)

**Плюсы:**
- ✅ Всегда свежие данные из сервера
- ✅ Нет проблем с устаревшими данными
- ✅ Простая логика

**Минусы:**
- ❌ Каждый раз нужно ждать загрузки из API (медленно)
- ❌ Нет мгновенной загрузки при повторном открытии
- ❌ Больше нагрузка на сервер

**Вывод:** ❌ Не рекомендуется

### Вариант 2: Отключить только localStorage, оставить Memory Cache

**Плюсы:**
- ✅ Мгновенная загрузка в рамках сессии (Memory Cache)
- ✅ Статические файлы кэшируются браузером (nginx)
- ✅ Нет проблем с устаревшими данными в localStorage
- ✅ Всегда свежие данные при новом открытии (из статических файлов)

**Минусы:**
- ⚠️ При первом открытии нужно загрузить статический файл (но это быстро через nginx)

**Вывод:** ✅ **РЕКОМЕНДУЕТСЯ**

### Вариант 3: Использовать только статические файлы + Memory Cache

**Плюсы:**
- ✅ Мгновенная загрузка (статические файлы через nginx)
- ✅ Memory Cache для ускорения в рамках сессии
- ✅ Нет проблем с устаревшими данными
- ✅ Минимальная нагрузка на API

**Минусы:**
- ⚠️ Нужно обновлять статические файлы при изменении данных
- ⚠️ При первом открытии нужно загрузить файл (но это быстро)

**Вывод:** ✅ **ОПТИМАЛЬНО**

## 🚀 Рекомендуемое решение

### Архитектура

1. **Статические JSON файлы** (основной источник)
   - Генерируются автоматически при обновлении серверного кэша
   - Кэшируются nginx (мгновенная загрузка)
   - Обновляются при изменении данных

2. **Memory Cache** (для текущей сессии)
   - Хранит данные только в памяти JavaScript
   - Очищается при закрытии страницы
   - Ускоряет повторные запросы в рамках сессии

3. **API** (fallback + обновление)
   - Используется только если статический файл недоступен
   - Используется для фонового обновления (stale-while-revalidate)

4. **localStorage** (отключен для планов)
   - Не используется для планов (статические файлы уже кэшируются браузером)
   - Можно оставить для других данных (если нужно)

### Преимущества

1. **Мгновенная загрузка**: Статические файлы отдаются nginx напрямую (< 50ms)
2. **Актуальность**: Файлы обновляются при изменении серверного кэша
3. **Надежность**: API используется только как fallback
4. **Производительность**: Минимальная нагрузка на сервер

## 📝 План реализации

### Шаг 1: Модифицировать data-loader.js

```javascript
// Убрать агрессивную очистку
// Отключить localStorage для планов
// Приоритет: Static JSON → Memory Cache → API
```

### Шаг 2: Автоматическое обновление статических файлов

```javascript
// При вызове /api/cache/prefill
// Автоматически генерировать статические JSON файлы
// Сохранять их в public/data/
```

### Шаг 3: Настроить nginx для кэширования статических файлов

```nginx
# Кэширование JSON файлов
location /data/ {
    proxy_cache_valid 200 1h;
    add_header Cache-Control "public, max-age=3600";
}
```

## 🔍 Сравнение вариантов

| Вариант | Скорость загрузки | Актуальность | Нагрузка на сервер | Сложность |
|---------|-------------------|--------------|-------------------|-----------|
| Текущий (с агрессивной очисткой) | Медленно (API) | ✅ Актуально | Высокая | Средняя |
| Без клиентского кэша | Медленно (API) | ✅ Актуально | Очень высокая | Низкая |
| Только Memory Cache | Быстро (сессия) | ✅ Актуально | Средняя | Средняя |
| Статические файлы + Memory | ⚡ Мгновенно | ✅ Актуально | Низкая | Средняя |

## ✅ Рекомендация

**Использовать статические JSON файлы + Memory Cache**

Это обеспечит:
- ⚡ Мгновенную загрузку для пользователей
- ✅ Актуальность данных (обновление при изменении кэша)
- 📉 Минимальную нагрузку на сервер
- 🎯 Простую логику кэширования
