# 🚀 Настройка автодеплоя с GitHub на Contabo VPS

## Что это дает:

При каждом push в ветку `main` на GitHub, сервер автоматически:
- Получает последние изменения
- Устанавливает зависимости (если нужно)
- Перезапускает Node.js сервер
- Обновляет Nginx (если конфигурация изменилась)

## Шаг 1: Настройка на сервере

### 1.1. Обновите проект на сервере

```bash
ssh root@37.60.228.11
cd /var/www/esimsdata
git pull origin main
npm install --production
```

### 1.2. Добавьте секретный ключ в .env

```bash
nano .env
```

Добавьте строки:

```
GITHUB_WEBHOOK_SECRET=ваш_секретный_ключ_здесь
PROJECT_DIR=/var/www/esimsdata
```

**Создайте секретный ключ:**
```bash
# На сервере сгенерируйте случайный ключ
openssl rand -hex 32
```

Скопируйте сгенерированный ключ и вставьте в `.env` файл.

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

### 1.3. Перезапустите сервер

```bash
pm2 restart esimsdata
```

## Шаг 2: Настройка GitHub Webhook

### 2.1. Откройте настройки репозитория

1. Перейдите на GitHub: https://github.com/come2me2/eSIMsData
2. Нажмите **Settings** (Настройки)
3. В левом меню выберите **Webhooks**
4. Нажмите **Add webhook** (Добавить webhook)

### 2.2. Заполните форму webhook

- **Payload URL**: `https://esimsdata.app/api/webhook`
- **Content type**: `application/json`
- **Secret**: Вставьте тот же секретный ключ, который вы добавили в `.env`
- **Which events would you like to trigger this webhook?**: Выберите **Just the push event**
- **Active**: ✅ (включено)

### 2.3. Сохраните webhook

Нажмите **Add webhook** (Добавить webhook)

## Шаг 3: Проверка работы

### 3.1. Тестовый push

Сделайте небольшое изменение в репозитории и запушьте:

```bash
# На вашем Mac
cd /Users/sergeykalinin/Desktop/eSim
echo "# Test" >> test.txt
git add test.txt
git commit -m "Test auto-deploy"
git push origin main
```

### 3.2. Проверьте логи на сервере

```bash
ssh root@37.60.228.11
pm2 logs esimsdata --lines 50
```

Вы должны увидеть сообщения о деплое:
- `📨 Получен webhook от GitHub`
- `🚀 Начинаю деплой...`
- `📥 Получаю изменения из GitHub...`
- `✅ Деплой завершен успешно!`

### 3.3. Проверьте статус webhook на GitHub

1. Вернитесь в **Settings → Webhooks** на GitHub
2. Нажмите на ваш webhook
3. Прокрутите вниз до **Recent Deliveries**
4. Вы должны увидеть успешные запросы (зеленые галочки)

## Шаг 4: Проверка безопасности

### 4.1. Проверьте подпись

Webhook проверяет подпись GitHub для безопасности. Убедитесь, что:
- `GITHUB_WEBHOOK_SECRET` установлен в `.env`
- Тот же секрет указан в настройках GitHub webhook

### 4.2. Проверьте ветку

По умолчанию деплой происходит только при push в ветку `main` или `master`.

## Troubleshooting

### Проблема: Webhook не срабатывает

1. Проверьте логи на сервере:
   ```bash
   pm2 logs esimsdata
   ```

2. Проверьте настройки webhook на GitHub:
   - URL правильный?
   - Secret совпадает?
   - События настроены правильно?

3. Проверьте доступность endpoint:
   ```bash
   curl -X POST https://esimsdata.app/api/webhook \
     -H "Content-Type: application/json" \
     -d '{"ref":"refs/heads/main"}'
   ```

### Проблема: Ошибка при деплое

1. Проверьте права доступа:
   ```bash
   ls -la /var/www/esimsdata
   chown -R www-data:www-data /var/www/esimsdata
   ```

2. Проверьте, что git настроен:
   ```bash
   cd /var/www/esimsdata
   git status
   ```

3. Проверьте логи:
   ```bash
   pm2 logs esimsdata --lines 100
   ```

### Проблема: Неверная подпись

1. Убедитесь, что секрет одинаковый в `.env` и на GitHub
2. Перезапустите сервер:
   ```bash
   pm2 restart esimsdata
   ```

## Дополнительные настройки

### Игнорирование определенных файлов

Если вы не хотите перезапускать сервер при изменении определенных файлов, можно добавить проверку в `api/webhook.js`.

### Уведомления

Можно добавить отправку уведомлений о статусе деплоя (Telegram, email и т.д.).

## Готово! 🎉

Теперь при каждом push в `main` ветку, сервер автоматически обновится!

