# Моментальная загрузка данных - Инструкция по установке

## Обзор решения

Эта оптимизация делает загрузку списка стран, регионов и тарифов **моментальной** для всех пользователей.

### Архитектура

```
Пользователь
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│  Браузер                                                │
│  ┌──────────────┐    ┌────────────────┐                 │
│  │ Memory Cache │ ◄─►│ localStorage   │                 │
│  │ (мгновенно)  │    │ (мгновенно)    │                 │
│  └──────┬───────┘    └────────────────┘                 │
└─────────┼───────────────────────────────────────────────┘
          │ miss
          ▼
┌─────────────────────────────────────────────────────────┐
│  Nginx                                                  │
│  ┌──────────────────┐    ┌──────────────────┐          │
│  │ /data/*.json     │    │ proxy_cache      │          │
│  │ (статика, ~5ms)  │    │ (API, ~10ms)     │          │
│  └──────────────────┘    └──────────────────┘          │
└─────────────────────────────────────────────────────────┘
          │ miss
          ▼
┌─────────────────────────────────────────────────────────┐
│  Node.js Server                                         │
│  ┌──────────────────┐                                   │
│  │ In-memory cache  │ ◄── Прогрев при старте            │
│  │ (из JSON файлов) │                                   │
│  └──────────────────┘                                   │
└─────────────────────────────────────────────────────────┘
          │ miss
          ▼
┌─────────────────────────────────────────────────────────┐
│  eSIM Go API (внешний, 1-10 сек)                        │
└─────────────────────────────────────────────────────────┘
```

## Шаги установки на Contabo

### 1. Загрузите новые файлы на сервер

```bash
# С локальной машины
scp scripts/generate-static-data.js root@your-server:/var/www/esimsdata/scripts/
scp public/data-loader.js root@your-server:/var/www/esimsdata/public/
scp server.js root@your-server:/var/www/esimsdata/
scp nginx.conf root@your-server:/var/www/esimsdata/

# Или используйте git pull если проект в git
ssh root@your-server
cd /var/www/esimsdata
git pull
```

### 2. Создайте директорию для статических данных

```bash
ssh root@your-server
mkdir -p /var/www/esimsdata/public/data
chown -R www-data:www-data /var/www/esimsdata/public/data
```

### 3. Сгенерируйте статические JSON файлы

```bash
cd /var/www/esimsdata
node scripts/generate-static-data.js
```

Это создаст файлы:
- `public/data/countries.json` - список стран
- `public/data/plans-global.json` - глобальные тарифы
- `public/data/plans-region-*.json` - региональные тарифы
- `public/data/plans-local-*.json` - локальные тарифы для популярных стран

### 4. Настройте nginx

```bash
# Создайте директорию для кэша
mkdir -p /var/cache/nginx/esim
chown -R www-data:www-data /var/cache/nginx/esim

# Замените конфигурацию (измените your-domain.com на ваш домен!)
nano /etc/nginx/sites-available/esimsdata

# Проверьте конфигурацию
nginx -t

# Перезагрузите nginx
systemctl reload nginx
```

### 5. Перезапустите сервер Node.js

```bash
cd /var/www/esimsdata
pm2 restart esimsdata

# Проверьте логи (должен быть прогрев кэша)
pm2 logs esimsdata --lines 50
```

Вы должны увидеть:
```
🔥 Warming up cache from static files...
✅ Cache warmed up: XX entries in XXms
```

### 6. Настройте автоматическое обновление данных (cron)

```bash
# Откройте crontab
crontab -e

# Добавьте строку (обновление каждый день в 3:00 ночи)
0 3 * * * cd /var/www/esimsdata && /usr/bin/node scripts/generate-static-data.js >> /var/log/esim-data-refresh.log 2>&1

# После обновления данных перезапустить сервер для прогрева кэша
5 3 * * * cd /var/www/esimsdata && /usr/bin/pm2 restart esimsdata
```

## Проверка работы

### 1. Проверьте статические файлы

```bash
curl -I https://your-domain.com/data/countries.json
```

Должен вернуть:
```
HTTP/2 200
content-type: application/json
cache-control: public, max-age=14400
```

### 2. Проверьте кэширование API

```bash
curl -I https://your-domain.com/api/esimgo/countries
```

При первом запросе:
```
X-Cache-Status: MISS
```

При повторном:
```
X-Cache-Status: HIT
```

### 3. Проверьте время загрузки в браузере

Откройте DevTools → Network → загрузите страницу:
- `/data/countries.json` должен загружаться < 50ms
- Повторные загрузки (из localStorage) - мгновенно

## Устранение проблем

### Данные не обновляются

```bash
# Очистите кэш nginx
rm -rf /var/cache/nginx/esim/*
nginx -s reload

# Перегенерируйте данные
cd /var/www/esimsdata
node scripts/generate-static-data.js

# Перезапустите сервер
pm2 restart esimsdata
```

### Ошибки при генерации данных

```bash
# Проверьте переменные окружения
cat /var/www/esimsdata/.env | grep ESIMGO

# Проверьте логи
tail -f /var/log/esim-data-refresh.log
```

### Очистка клиентского кэша

В консоли браузера:
```javascript
DataLoader.clearCache();
location.reload();
```

## Метрики

После внедрения вы должны увидеть:

| Метрика | До | После |
|---------|-----|--------|
| Первая загрузка списка стран | 2-10 сек | < 100 мс |
| Повторная загрузка | 2-10 сек | < 10 мс |
| Загрузка тарифов | 3-15 сек | < 100 мс |

## Файлы изменений

- `scripts/generate-static-data.js` - генератор статических данных
- `public/data-loader.js` - клиентский загрузчик с кэшированием
- `server.js` - прогрев кэша при старте
- `nginx.conf` - кэширование на уровне nginx
- `app.js` - использование DataLoader
- `plans-loader.js` - использование DataLoader
- `*.html` - подключение data-loader.js



## Обзор решения

Эта оптимизация делает загрузку списка стран, регионов и тарифов **моментальной** для всех пользователей.

### Архитектура

```
Пользователь
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│  Браузер                                                │
│  ┌──────────────┐    ┌────────────────┐                 │
│  │ Memory Cache │ ◄─►│ localStorage   │                 │
│  │ (мгновенно)  │    │ (мгновенно)    │                 │
│  └──────┬───────┘    └────────────────┘                 │
└─────────┼───────────────────────────────────────────────┘
          │ miss
          ▼
┌─────────────────────────────────────────────────────────┐
│  Nginx                                                  │
│  ┌──────────────────┐    ┌──────────────────┐          │
│  │ /data/*.json     │    │ proxy_cache      │          │
│  │ (статика, ~5ms)  │    │ (API, ~10ms)     │          │
│  └──────────────────┘    └──────────────────┘          │
└─────────────────────────────────────────────────────────┘
          │ miss
          ▼
┌─────────────────────────────────────────────────────────┐
│  Node.js Server                                         │
│  ┌──────────────────┐                                   │
│  │ In-memory cache  │ ◄── Прогрев при старте            │
│  │ (из JSON файлов) │                                   │
│  └──────────────────┘                                   │
└─────────────────────────────────────────────────────────┘
          │ miss
          ▼
┌─────────────────────────────────────────────────────────┐
│  eSIM Go API (внешний, 1-10 сек)                        │
└─────────────────────────────────────────────────────────┘
```

## Шаги установки на Contabo

### 1. Загрузите новые файлы на сервер

```bash
# С локальной машины
scp scripts/generate-static-data.js root@your-server:/var/www/esimsdata/scripts/
scp public/data-loader.js root@your-server:/var/www/esimsdata/public/
scp server.js root@your-server:/var/www/esimsdata/
scp nginx.conf root@your-server:/var/www/esimsdata/

# Или используйте git pull если проект в git
ssh root@your-server
cd /var/www/esimsdata
git pull
```

### 2. Создайте директорию для статических данных

```bash
ssh root@your-server
mkdir -p /var/www/esimsdata/public/data
chown -R www-data:www-data /var/www/esimsdata/public/data
```

### 3. Сгенерируйте статические JSON файлы

```bash
cd /var/www/esimsdata
node scripts/generate-static-data.js
```

Это создаст файлы:
- `public/data/countries.json` - список стран
- `public/data/plans-global.json` - глобальные тарифы
- `public/data/plans-region-*.json` - региональные тарифы
- `public/data/plans-local-*.json` - локальные тарифы для популярных стран

### 4. Настройте nginx

```bash
# Создайте директорию для кэша
mkdir -p /var/cache/nginx/esim
chown -R www-data:www-data /var/cache/nginx/esim

# Замените конфигурацию (измените your-domain.com на ваш домен!)
nano /etc/nginx/sites-available/esimsdata

# Проверьте конфигурацию
nginx -t

# Перезагрузите nginx
systemctl reload nginx
```

### 5. Перезапустите сервер Node.js

```bash
cd /var/www/esimsdata
pm2 restart esimsdata

# Проверьте логи (должен быть прогрев кэша)
pm2 logs esimsdata --lines 50
```

Вы должны увидеть:
```
🔥 Warming up cache from static files...
✅ Cache warmed up: XX entries in XXms
```

### 6. Настройте автоматическое обновление данных (cron)

```bash
# Откройте crontab
crontab -e

# Добавьте строку (обновление каждый день в 3:00 ночи)
0 3 * * * cd /var/www/esimsdata && /usr/bin/node scripts/generate-static-data.js >> /var/log/esim-data-refresh.log 2>&1

# После обновления данных перезапустить сервер для прогрева кэша
5 3 * * * cd /var/www/esimsdata && /usr/bin/pm2 restart esimsdata
```

## Проверка работы

### 1. Проверьте статические файлы

```bash
curl -I https://your-domain.com/data/countries.json
```

Должен вернуть:
```
HTTP/2 200
content-type: application/json
cache-control: public, max-age=14400
```

### 2. Проверьте кэширование API

```bash
curl -I https://your-domain.com/api/esimgo/countries
```

При первом запросе:
```
X-Cache-Status: MISS
```

При повторном:
```
X-Cache-Status: HIT
```

### 3. Проверьте время загрузки в браузере

Откройте DevTools → Network → загрузите страницу:
- `/data/countries.json` должен загружаться < 50ms
- Повторные загрузки (из localStorage) - мгновенно

## Устранение проблем

### Данные не обновляются

```bash
# Очистите кэш nginx
rm -rf /var/cache/nginx/esim/*
nginx -s reload

# Перегенерируйте данные
cd /var/www/esimsdata
node scripts/generate-static-data.js

# Перезапустите сервер
pm2 restart esimsdata
```

### Ошибки при генерации данных

```bash
# Проверьте переменные окружения
cat /var/www/esimsdata/.env | grep ESIMGO

# Проверьте логи
tail -f /var/log/esim-data-refresh.log
```

### Очистка клиентского кэша

В консоли браузера:
```javascript
DataLoader.clearCache();
location.reload();
```

## Метрики

После внедрения вы должны увидеть:

| Метрика | До | После |
|---------|-----|--------|
| Первая загрузка списка стран | 2-10 сек | < 100 мс |
| Повторная загрузка | 2-10 сек | < 10 мс |
| Загрузка тарифов | 3-15 сек | < 100 мс |

## Файлы изменений

- `scripts/generate-static-data.js` - генератор статических данных
- `public/data-loader.js` - клиентский загрузчик с кэшированием
- `server.js` - прогрев кэша при старте
- `nginx.conf` - кэширование на уровне nginx
- `app.js` - использование DataLoader
- `plans-loader.js` - использование DataLoader
- `*.html` - подключение data-loader.js




































