# 📱 План реализации отправки SMS при расходе трафика

## Цель
Настроить автоматическую отправку SMS на приобретенные eSIM при расходе 80% и 100% трафика через eSIM Go API.

## Текущее состояние

### ✅ Что уже есть:
1. **Callback endpoint** (`/api/esimgo/callback`) - обрабатывает уведомления от eSIM Go о использовании трафика
2. **eSIM Go API Client** (`api/_lib/esimgo/client.js`) - базовый клиент для работы с API
3. **Обновление статистики** - callback уже обновляет данные об использовании трафика в заказах

### ❌ Что нужно добавить:
1. Функция отправки SMS через eSIM Go API
2. Логика определения процента использования трафика (80% и 100%)
3. Защита от повторной отправки SMS для одного порога
4. Конфигурация текстов сообщений
5. Обработка ошибок при отправке SMS

## Технические детали

### eSIM Go API для отправки SMS
**Endpoint:** `POST /v2.4/esims/{iccid}/sms`

**Параметры:**
- `message` (string, 1-160 символов, UTF-8) - текст сообщения
- `from` (string, по умолчанию "eSIM") - отправитель

**Документация:** https://docs.esim-go.com/api/v2_4/#tag/eSIMs/operation/sendSMS

### Callback данные от eSIM Go
При получении callback с `alertType: "Utilisation"` доступны:
- `bundle.initialQuantity` - начальное количество трафика (в байтах)
- `bundle.remainingQuantity` - оставшееся количество трафика (в байтах)
- `bundle.name` - название bundle
- `iccid` - ICCID eSIM

**Расчет процента использования:**
```javascript
const usedQuantity = initialQuantity - remainingQuantity;
const usagePercent = (usedQuantity / initialQuantity) * 100;
```

## План реализации

### Этап 1: Создание функции отправки SMS
**Файл:** `api/esimgo/send-sms.js` (новый)

**Функционал:**
- Функция `sendSMSToESIM(iccid, message, from = 'eSIM')`
- Использование существующего `api/_lib/esimgo/client.js` для запросов
- Обработка ошибок и логирование
- Валидация длины сообщения (1-160 символов)

**Пример использования:**
```javascript
const sendSMSToESIM = require('./api/esimgo/send-sms');

await sendSMSToESIM(iccid, 'Ваш трафик на 80%!', 'eSIM');
```

### Этап 2: Модификация callback handler
**Файл:** `api/esimgo/callback.js`

**Изменения:**
1. Импорт функции отправки SMS
2. Расчет процента использования трафика
3. Проверка достижения порогов (80% и 100%)
4. Проверка, не была ли уже отправлена SMS для этого порога
5. Отправка SMS при достижении порога
6. Сохранение информации об отправленных SMS в заказе

**Логика проверки порогов:**
```javascript
const usagePercent = calculateUsagePercent(bundle);
const thresholds = [80, 100];

for (const threshold of thresholds) {
    if (usagePercent >= threshold && !wasSmsSent(order, threshold)) {
        await sendUsageSMS(iccid, threshold, bundle);
        markSmsAsSent(order, threshold);
    }
}
```

### Этап 3: Хранение информации об отправленных SMS
**Структура данных в заказе:**
```json
{
  "orderReference": "...",
  "iccid": "...",
  "usage": {
    "bundle": "...",
    "initialQuantity": 1000000000,
    "remainingQuantity": 150000000,
    "smsSent": {
      "80": {
        "sent": true,
        "sentAt": "2025-01-13T12:00:00Z"
      },
      "100": {
        "sent": false
      }
    }
  }
}
```

### Этап 4: Конфигурация текстов сообщений
**Варианты:**
1. **В коде** (простой вариант) - константы в `callback.js`
2. **В settings** (гибкий вариант) - через админ-панель
3. **В отдельном файле** (средний вариант) - `config/sms-messages.js`

**Рекомендация:** Начать с констант в коде, позже можно вынести в settings.

**Структура:**
```javascript
const SMS_MESSAGES = {
    '80': 'Ваш трафик на 80%! Осталось {remaining} МБ. Продлите тариф в приложении.',
    '100': 'Ваш трафик закончился. Купите новый тариф в приложении.'
};
```

### Этап 5: Обработка edge cases
1. **Бесконечный bundle** (`bundle.unlimited === true`) - не отправлять SMS
2. **Ошибка отправки SMS** - логировать, но не прерывать обработку callback
3. **Повторные callback'и** - проверять, не была ли уже отправлена SMS
4. **Нет ICCID** - не отправлять SMS
5. **Нет данных о bundle** - не отправлять SMS

## Структура файлов

```
api/
├── esimgo/
│   ├── callback.js          # Модифицировать - добавить логику отправки SMS
│   └── send-sms.js          # Новый файл - функция отправки SMS
└── _lib/
    └── esimgo/
        └── client.js         # Использовать существующий клиент
```

## Последовательность реализации

1. ✅ **Создать `api/esimgo/send-sms.js`**
   - Функция отправки SMS через eSIM Go API
   - Тестирование функции

2. ✅ **Модифицировать `api/esimgo/callback.js`**
   - Добавить расчет процента использования
   - Добавить проверку порогов 80% и 100%
   - Добавить отправку SMS
   - Добавить сохранение информации об отправленных SMS

3. ✅ **Добавить конфигурацию текстов**
   - Создать константы с текстами сообщений
   - Добавить подстановку переменных (оставшийся трафик)

4. ✅ **Тестирование**
   - Тест с mock данными
   - Тест с реальным callback от eSIM Go
   - Проверка защиты от повторной отправки

5. ✅ **Документация**
   - Обновить документацию по callback
   - Добавить инструкцию по настройке текстов

## Примеры текстов сообщений (для пользователя)

**80% трафика:**
```
Ваш трафик на 80%! Осталось {remaining} МБ. Продлите тариф в приложении.
```

**100% трафика:**
```
Ваш трафик закончился. Купите новый тариф в приложении.
```

*Примечание: Тексты будут предоставлены пользователем позже.*

## Настройка в eSIM Portal

Убедиться, что в eSIM Portal настроен Callback URL:
- **Account Settings → API Details → Callback URL:** `https://your-domain.com/api/esimgo/callback`
- **Callback Version:** V3 (рекомендуется) или V2

## Безопасность

1. Валидация HMAC подписи (уже реализована)
2. Проверка наличия ICCID перед отправкой SMS
3. Ограничение длины сообщения (1-160 символов)
4. Логирование всех отправок SMS для аудита

## Мониторинг

Добавить логирование:
- `📱 SMS sent to ICCID {iccid} at {threshold}% usage`
- `⚠️ Failed to send SMS to ICCID {iccid}: {error}`
- `✅ SMS already sent for threshold {threshold}%`

## Следующие шаги

1. Дождаться текстов сообщений от пользователя
2. Реализовать функцию отправки SMS
3. Интегрировать в callback handler
4. Протестировать на реальных данных
5. Развернуть на сервере
