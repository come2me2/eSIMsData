# 💫 Пошаговый план подключения Telegram Stars

## 📊 Текущий статус

### ✅ Что уже готово:
1. **API endpoints созданы:**
   - `/api/telegram/stars/create-invoice` - создание инвойса
   - `/api/telegram/stars/webhook` - обработка платежей
   - Маршруты зарегистрированы в `server.js`

2. **UI для выбора метода оплаты:**
   - Карточка "Payment method" на странице checkout
   - Bottom sheet с выбором методов (Telegram Stars, Bank Cards, Crypto Payments)
   - Сохранение выбора в localStorage

3. **Серверная логика:**
   - Конвертация USD → Stars
   - Создание инвойсов через Telegram Bot API
   - Обработка webhook'ов от Telegram
   - Автоматическое создание заказов в eSIM Go после оплаты

### ❌ Что нужно доделать:
1. **Клиентская часть (checkout.js):**
   - Проверка выбранного метода оплаты
   - Вызов API для создания инвойса
   - Открытие инвойса через `tg.openInvoice()`
   - Обработка результата оплаты

2. **Настройка инфраструктуры:**
   - Получить токен Telegram бота
   - Настроить переменные окружения на сервере
   - Настроить webhook для бота (или использовать long polling)

---

## 🎯 Пошаговый план действий

### Этап 1: Настройка Telegram бота (от тебя)

#### Шаг 1.1: Создать бота (5 минут)
1. Открой [@BotFather](https://t.me/BotFather) в Telegram
2. Отправь команду `/newbot`
3. Следуй инструкциям:
   - Введи имя бота (например: `eSimsData Bot`)
   - Введи username бота (например: `esimsdata_bot`)
4. **Сохрани токен бота** (формат: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)
   - ⚠️ **Важно**: Токен секретный, не публикуй его!

#### Шаг 1.2: Проверить поддержку Stars (2 минуты)
1. В @BotFather отправь `/mybots`
2. Выбери своего бота
3. Проверь, что бот поддерживает последнюю версию Bot API
4. Если нужно - обнови бота через BotFather

#### Шаг 1.3: Предоставить токен (1 минута)
- **Отправь мне токен бота** через безопасный канал (не в публичном чате!)
- Или добавь его в переменные окружения на сервере (см. Этап 2)

---

### Этап 2: Настройка сервера (от тебя + я помогу)

#### Шаг 2.1: Добавить переменные окружения на сервере

**Вариант A: Если у тебя есть доступ к серверу:**
```bash
# Подключись к серверу
ssh root@37.60.228.11

# Отредактируй .env файл (или создай, если нет)
nano /var/www/esimsdata/.env

# Добавь следующие строки:
TELEGRAM_BOT_TOKEN=твой_токен_бота_здесь
STARS_RATE=0.013
STARS_MARGIN=0.29
STARS_TELEGRAM_FEE=0.25
TELEGRAM_WEBHOOK_SECRET=случайная_секретная_строка_для_безопасности
```

**Вариант B: Если нет доступа - отправь мне токен, я добавлю**

#### Шаг 2.2: Перезапустить сервер (я сделаю)
После добавления переменных окружения нужно перезапустить Node.js процесс:
```bash
pm2 restart esimsdata
# или
systemctl restart esimsdata
```

---

### Этап 3: Настройка Webhook (я сделаю после получения токена)

#### Шаг 3.1: Настроить webhook для бота
Есть два варианта:

**Вариант A: Webhook (рекомендуется для продакшн)**
- URL: `https://esimsdata.app/api/telegram/stars/webhook`
- Нужен HTTPS сертификат (у тебя уже есть)
- Telegram будет отправлять события платежей на этот URL

**Вариант B: Long Polling (для тестирования)**
- Не требует настройки webhook
- Бот будет периодически опрашивать Telegram API
- Медленнее, но проще для тестирования

**Я настрою webhook автоматически после получения токена.**

---

### Этап 4: Разработка клиентской части (я сделаю)

#### Шаг 4.1: Модифицировать `checkout.js`
Нужно добавить:
1. Проверку выбранного метода оплаты (`selectedPaymentMethod === 'stars'`)
2. Функцию `initiateStarsPayment()`:
   - Получение данных тарифа
   - Вызов `/api/telegram/stars/create-invoice`
   - Открытие инвойса через `tg.openInvoice()`
3. Обработку результата:
   - Успешная оплата → показать QR код
   - Отмена → вернуться на checkout
   - Ошибка → показать сообщение

#### Шаг 4.2: Обновить `setupPurchaseButton()`
- Проверять `selectedPaymentMethod`
- Если `'stars'` → вызывать `initiateStarsPayment()`
- Если другой метод → показать сообщение "Coming soon" или использовать текущий процесс

**Это займет ~1-2 часа разработки.**

---

### Этап 5: Тестирование (вместе)

#### Шаг 5.1: Тестовый платеж
1. Открой checkout страницу в Telegram
2. Выбери тариф
3. Выбери "Telegram Stars" как метод оплаты
4. Нажми "Purchase"
5. Должно открыться окно оплаты Telegram Stars
6. Оплати тестовыми Stars (если есть) или реальными
7. Проверь, что:
   - Платеж обработан
   - Заказ создан в eSIM Go
   - QR код получен и отображен

#### Шаг 5.2: Проверка логов
- Проверить логи сервера на ошибки
- Проверить логи Telegram Bot API
- Убедиться, что webhook получает события

---

## 📋 Чек-лист для тебя

### Сейчас (5-10 минут):
- [ ] Создать бота через @BotFather
- [ ] Сохранить токен бота
- [ ] Отправить мне токен (или добавить в .env на сервере)

### После получения токена (я сделаю):
- [ ] Добавить токен в переменные окружения
- [ ] Настроить webhook для бота
- [ ] Разработать клиентскую часть
- [ ] Протестировать интеграцию

### Перед запуском:
- [ ] Протестировать тестовый платеж
- [ ] Проверить создание заказов
- [ ] Проверить получение QR кодов

---

## 🔧 Технические детали

### Конвертация цен

**Формула расчета Stars:**
```
Stars = (price / (1 - margin) / (1 - telegram_fee) / stars_rate)
```

**Параметры:**
- `price` - **себестоимость** тарифа в USD (cost, не финальная цена для пользователя!)
  - Например, для `esim_1GB_7D_AE_V2` себестоимость = $2.26
  - ⚠️ **Важно**: Используй себестоимость, а не цену с маржой!
- `margin` - наша маржа в долях (0.29 = 29%)
- `telegram_fee` - комиссия Telegram в долях (0.25 = 25%)
- `stars_rate` - курс 1 Star в USD (0.013 = $0.013)

**Пример расчета:**
Для тарифа `esim_1GB_7D_AE_V2` с себестоимостью $2.26:
```
Stars = (2.26 / (1 - 0.29) / (1 - 0.25) / 0.013) ≈ 326 Stars
```

**Переменные окружения:**
- `STARS_RATE=0.013` - курс 1 Star = $0.013
- `STARS_MARGIN=0.29` - наша маржа 29%
- `STARS_TELEGRAM_FEE=0.25` - комиссия Telegram 25%

**Округление**: Вверх до целого числа Stars  
**Минимум**: 1 Star

### Структура payload инвойса
```json
{
  "pid": "plan_id",
  "pt": "standard|unlimited",
  "bn": "bundle_name",
  "cc": "country_code",
  "cn": "country_name",
  "uid": "telegram_user_id",
  "amt": 1000,
  "cur": "XTR"
}
```

### Процесс оплаты
1. Пользователь выбирает тариф → нажимает "Purchase"
2. Если выбран "Telegram Stars":
   - Клиент вызывает `/api/telegram/stars/create-invoice`
   - Сервер создает инвойс через Telegram Bot API
   - Возвращает ссылку на инвойс
3. Клиент открывает инвойс через `tg.openInvoice(link)`
4. Пользователь оплачивает в Telegram
5. Telegram отправляет webhook на `/api/telegram/stars/webhook`
6. Сервер обрабатывает платеж и создает заказ в eSIM Go
7. Пользователь получает QR код

---

## ⚠️ Важные замечания

1. **Безопасность токена:**
   - Никогда не публикуй токен в открытом доступе
   - Не коммить токен в git
   - Используй переменные окружения

2. **Комиссии Telegram:**
   - Telegram берет комиссию 25% (учитывается в формуле)
   - Комиссии Apple/Google (30%) уже учтены в курсе $0.013
   - Наша маржа 29% также учитывается в формуле
   - Все параметры настраиваются через переменные окружения

3. **Тестирование:**
   - Для тестирования можно использовать тестовые Stars
   - Или реальные Stars на небольшой сумме
   - Проверь все сценарии (успех, отмена, ошибка)

4. **Мониторинг:**
   - Настрой мониторинг логов сервера
   - Отслеживай успешные/неуспешные платежи
   - Проверяй создание заказов в eSIM Go

---

## 🚀 Следующие шаги

1. **Ты делаешь:**
   - Создаешь бота и получаешь токен
   - Отправляешь мне токен

2. **Я делаю:**
   - Добавляю токен в переменные окружения
   - Настраиваю webhook
   - Разрабатываю клиентскую часть
   - Тестирую интеграцию

3. **Вместе:**
   - Тестируем полный цикл оплаты
   - Проверяем все edge cases
   - Запускаем в продакшн

---

## 📞 Вопросы?

Если что-то непонятно или нужна помощь:
- Напиши мне
- Проверь документацию: https://core.telegram.org/bots/payments-stars
- Посмотри примеры в коде: `api/telegram/stars/`

**Готов начать, как только получишь токен бота!** 🚀


## 📊 Текущий статус

### ✅ Что уже готово:
1. **API endpoints созданы:**
   - `/api/telegram/stars/create-invoice` - создание инвойса
   - `/api/telegram/stars/webhook` - обработка платежей
   - Маршруты зарегистрированы в `server.js`

2. **UI для выбора метода оплаты:**
   - Карточка "Payment method" на странице checkout
   - Bottom sheet с выбором методов (Telegram Stars, Bank Cards, Crypto Payments)
   - Сохранение выбора в localStorage

3. **Серверная логика:**
   - Конвертация USD → Stars
   - Создание инвойсов через Telegram Bot API
   - Обработка webhook'ов от Telegram
   - Автоматическое создание заказов в eSIM Go после оплаты

### ❌ Что нужно доделать:
1. **Клиентская часть (checkout.js):**
   - Проверка выбранного метода оплаты
   - Вызов API для создания инвойса
   - Открытие инвойса через `tg.openInvoice()`
   - Обработка результата оплаты

2. **Настройка инфраструктуры:**
   - Получить токен Telegram бота
   - Настроить переменные окружения на сервере
   - Настроить webhook для бота (или использовать long polling)

---

## 🎯 Пошаговый план действий

### Этап 1: Настройка Telegram бота (от тебя)

#### Шаг 1.1: Создать бота (5 минут)
1. Открой [@BotFather](https://t.me/BotFather) в Telegram
2. Отправь команду `/newbot`
3. Следуй инструкциям:
   - Введи имя бота (например: `eSimsData Bot`)
   - Введи username бота (например: `esimsdata_bot`)
4. **Сохрани токен бота** (формат: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)
   - ⚠️ **Важно**: Токен секретный, не публикуй его!

#### Шаг 1.2: Проверить поддержку Stars (2 минуты)
1. В @BotFather отправь `/mybots`
2. Выбери своего бота
3. Проверь, что бот поддерживает последнюю версию Bot API
4. Если нужно - обнови бота через BotFather

#### Шаг 1.3: Предоставить токен (1 минута)
- **Отправь мне токен бота** через безопасный канал (не в публичном чате!)
- Или добавь его в переменные окружения на сервере (см. Этап 2)

---

### Этап 2: Настройка сервера (от тебя + я помогу)

#### Шаг 2.1: Добавить переменные окружения на сервере

**Вариант A: Если у тебя есть доступ к серверу:**
```bash
# Подключись к серверу
ssh root@37.60.228.11

# Отредактируй .env файл (или создай, если нет)
nano /var/www/esimsdata/.env

# Добавь следующие строки:
TELEGRAM_BOT_TOKEN=твой_токен_бота_здесь
STARS_RATE=0.013
STARS_MARGIN=0.29
STARS_TELEGRAM_FEE=0.25
TELEGRAM_WEBHOOK_SECRET=случайная_секретная_строка_для_безопасности
```

**Вариант B: Если нет доступа - отправь мне токен, я добавлю**

#### Шаг 2.2: Перезапустить сервер (я сделаю)
После добавления переменных окружения нужно перезапустить Node.js процесс:
```bash
pm2 restart esimsdata
# или
systemctl restart esimsdata
```

---

### Этап 3: Настройка Webhook (я сделаю после получения токена)

#### Шаг 3.1: Настроить webhook для бота
Есть два варианта:

**Вариант A: Webhook (рекомендуется для продакшн)**
- URL: `https://esimsdata.app/api/telegram/stars/webhook`
- Нужен HTTPS сертификат (у тебя уже есть)
- Telegram будет отправлять события платежей на этот URL

**Вариант B: Long Polling (для тестирования)**
- Не требует настройки webhook
- Бот будет периодически опрашивать Telegram API
- Медленнее, но проще для тестирования

**Я настрою webhook автоматически после получения токена.**

---

### Этап 4: Разработка клиентской части (я сделаю)

#### Шаг 4.1: Модифицировать `checkout.js`
Нужно добавить:
1. Проверку выбранного метода оплаты (`selectedPaymentMethod === 'stars'`)
2. Функцию `initiateStarsPayment()`:
   - Получение данных тарифа
   - Вызов `/api/telegram/stars/create-invoice`
   - Открытие инвойса через `tg.openInvoice()`
3. Обработку результата:
   - Успешная оплата → показать QR код
   - Отмена → вернуться на checkout
   - Ошибка → показать сообщение

#### Шаг 4.2: Обновить `setupPurchaseButton()`
- Проверять `selectedPaymentMethod`
- Если `'stars'` → вызывать `initiateStarsPayment()`
- Если другой метод → показать сообщение "Coming soon" или использовать текущий процесс

**Это займет ~1-2 часа разработки.**

---

### Этап 5: Тестирование (вместе)

#### Шаг 5.1: Тестовый платеж
1. Открой checkout страницу в Telegram
2. Выбери тариф
3. Выбери "Telegram Stars" как метод оплаты
4. Нажми "Purchase"
5. Должно открыться окно оплаты Telegram Stars
6. Оплати тестовыми Stars (если есть) или реальными
7. Проверь, что:
   - Платеж обработан
   - Заказ создан в eSIM Go
   - QR код получен и отображен

#### Шаг 5.2: Проверка логов
- Проверить логи сервера на ошибки
- Проверить логи Telegram Bot API
- Убедиться, что webhook получает события

---

## 📋 Чек-лист для тебя

### Сейчас (5-10 минут):
- [ ] Создать бота через @BotFather
- [ ] Сохранить токен бота
- [ ] Отправить мне токен (или добавить в .env на сервере)

### После получения токена (я сделаю):
- [ ] Добавить токен в переменные окружения
- [ ] Настроить webhook для бота
- [ ] Разработать клиентскую часть
- [ ] Протестировать интеграцию

### Перед запуском:
- [ ] Протестировать тестовый платеж
- [ ] Проверить создание заказов
- [ ] Проверить получение QR кодов

---

## 🔧 Технические детали

### Конвертация цен

**Формула расчета Stars:**
```
Stars = (price / (1 - margin) / (1 - telegram_fee) / stars_rate)
```

**Параметры:**
- `price` - **себестоимость** тарифа в USD (cost, не финальная цена для пользователя!)
  - Например, для `esim_1GB_7D_AE_V2` себестоимость = $2.26
  - ⚠️ **Важно**: Используй себестоимость, а не цену с маржой!
- `margin` - наша маржа в долях (0.29 = 29%)
- `telegram_fee` - комиссия Telegram в долях (0.25 = 25%)
- `stars_rate` - курс 1 Star в USD (0.013 = $0.013)

**Пример расчета:**
Для тарифа `esim_1GB_7D_AE_V2` с себестоимостью $2.26:
```
Stars = (2.26 / (1 - 0.29) / (1 - 0.25) / 0.013) ≈ 326 Stars
```

**Переменные окружения:**
- `STARS_RATE=0.013` - курс 1 Star = $0.013
- `STARS_MARGIN=0.29` - наша маржа 29%
- `STARS_TELEGRAM_FEE=0.25` - комиссия Telegram 25%

**Округление**: Вверх до целого числа Stars  
**Минимум**: 1 Star

### Структура payload инвойса
```json
{
  "pid": "plan_id",
  "pt": "standard|unlimited",
  "bn": "bundle_name",
  "cc": "country_code",
  "cn": "country_name",
  "uid": "telegram_user_id",
  "amt": 1000,
  "cur": "XTR"
}
```

### Процесс оплаты
1. Пользователь выбирает тариф → нажимает "Purchase"
2. Если выбран "Telegram Stars":
   - Клиент вызывает `/api/telegram/stars/create-invoice`
   - Сервер создает инвойс через Telegram Bot API
   - Возвращает ссылку на инвойс
3. Клиент открывает инвойс через `tg.openInvoice(link)`
4. Пользователь оплачивает в Telegram
5. Telegram отправляет webhook на `/api/telegram/stars/webhook`
6. Сервер обрабатывает платеж и создает заказ в eSIM Go
7. Пользователь получает QR код

---

## ⚠️ Важные замечания

1. **Безопасность токена:**
   - Никогда не публикуй токен в открытом доступе
   - Не коммить токен в git
   - Используй переменные окружения

2. **Комиссии Telegram:**
   - Telegram берет комиссию 25% (учитывается в формуле)
   - Комиссии Apple/Google (30%) уже учтены в курсе $0.013
   - Наша маржа 29% также учитывается в формуле
   - Все параметры настраиваются через переменные окружения

3. **Тестирование:**
   - Для тестирования можно использовать тестовые Stars
   - Или реальные Stars на небольшой сумме
   - Проверь все сценарии (успех, отмена, ошибка)

4. **Мониторинг:**
   - Настрой мониторинг логов сервера
   - Отслеживай успешные/неуспешные платежи
   - Проверяй создание заказов в eSIM Go

---

## 🚀 Следующие шаги

1. **Ты делаешь:**
   - Создаешь бота и получаешь токен
   - Отправляешь мне токен

2. **Я делаю:**
   - Добавляю токен в переменные окружения
   - Настраиваю webhook
   - Разрабатываю клиентскую часть
   - Тестирую интеграцию

3. **Вместе:**
   - Тестируем полный цикл оплаты
   - Проверяем все edge cases
   - Запускаем в продакшн

---

## 📞 Вопросы?

Если что-то непонятно или нужна помощь:
- Напиши мне
- Проверь документацию: https://core.telegram.org/bots/payments-stars
- Посмотри примеры в коде: `api/telegram/stars/`

**Готов начать, как только получишь токен бота!** 🚀


## 📊 Текущий статус

### ✅ Что уже готово:
1. **API endpoints созданы:**
   - `/api/telegram/stars/create-invoice` - создание инвойса
   - `/api/telegram/stars/webhook` - обработка платежей
   - Маршруты зарегистрированы в `server.js`

2. **UI для выбора метода оплаты:**
   - Карточка "Payment method" на странице checkout
   - Bottom sheet с выбором методов (Telegram Stars, Bank Cards, Crypto Payments)
   - Сохранение выбора в localStorage

3. **Серверная логика:**
   - Конвертация USD → Stars
   - Создание инвойсов через Telegram Bot API
   - Обработка webhook'ов от Telegram
   - Автоматическое создание заказов в eSIM Go после оплаты

### ❌ Что нужно доделать:
1. **Клиентская часть (checkout.js):**
   - Проверка выбранного метода оплаты
   - Вызов API для создания инвойса
   - Открытие инвойса через `tg.openInvoice()`
   - Обработка результата оплаты

2. **Настройка инфраструктуры:**
   - Получить токен Telegram бота
   - Настроить переменные окружения на сервере
   - Настроить webhook для бота (или использовать long polling)

---

## 🎯 Пошаговый план действий

### Этап 1: Настройка Telegram бота (от тебя)

#### Шаг 1.1: Создать бота (5 минут)
1. Открой [@BotFather](https://t.me/BotFather) в Telegram
2. Отправь команду `/newbot`
3. Следуй инструкциям:
   - Введи имя бота (например: `eSimsData Bot`)
   - Введи username бота (например: `esimsdata_bot`)
4. **Сохрани токен бота** (формат: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)
   - ⚠️ **Важно**: Токен секретный, не публикуй его!

#### Шаг 1.2: Проверить поддержку Stars (2 минуты)
1. В @BotFather отправь `/mybots`
2. Выбери своего бота
3. Проверь, что бот поддерживает последнюю версию Bot API
4. Если нужно - обнови бота через BotFather

#### Шаг 1.3: Предоставить токен (1 минута)
- **Отправь мне токен бота** через безопасный канал (не в публичном чате!)
- Или добавь его в переменные окружения на сервере (см. Этап 2)

---

### Этап 2: Настройка сервера (от тебя + я помогу)

#### Шаг 2.1: Добавить переменные окружения на сервере

**Вариант A: Если у тебя есть доступ к серверу:**
```bash
# Подключись к серверу
ssh root@37.60.228.11

# Отредактируй .env файл (или создай, если нет)
nano /var/www/esimsdata/.env

# Добавь следующие строки:
TELEGRAM_BOT_TOKEN=твой_токен_бота_здесь
STARS_RATE=0.013
STARS_MARGIN=0.29
STARS_TELEGRAM_FEE=0.25
TELEGRAM_WEBHOOK_SECRET=случайная_секретная_строка_для_безопасности
```

**Вариант B: Если нет доступа - отправь мне токен, я добавлю**

#### Шаг 2.2: Перезапустить сервер (я сделаю)
После добавления переменных окружения нужно перезапустить Node.js процесс:
```bash
pm2 restart esimsdata
# или
systemctl restart esimsdata
```

---

### Этап 3: Настройка Webhook (я сделаю после получения токена)

#### Шаг 3.1: Настроить webhook для бота
Есть два варианта:

**Вариант A: Webhook (рекомендуется для продакшн)**
- URL: `https://esimsdata.app/api/telegram/stars/webhook`
- Нужен HTTPS сертификат (у тебя уже есть)
- Telegram будет отправлять события платежей на этот URL

**Вариант B: Long Polling (для тестирования)**
- Не требует настройки webhook
- Бот будет периодически опрашивать Telegram API
- Медленнее, но проще для тестирования

**Я настрою webhook автоматически после получения токена.**

---

### Этап 4: Разработка клиентской части (я сделаю)

#### Шаг 4.1: Модифицировать `checkout.js`
Нужно добавить:
1. Проверку выбранного метода оплаты (`selectedPaymentMethod === 'stars'`)
2. Функцию `initiateStarsPayment()`:
   - Получение данных тарифа
   - Вызов `/api/telegram/stars/create-invoice`
   - Открытие инвойса через `tg.openInvoice()`
3. Обработку результата:
   - Успешная оплата → показать QR код
   - Отмена → вернуться на checkout
   - Ошибка → показать сообщение

#### Шаг 4.2: Обновить `setupPurchaseButton()`
- Проверять `selectedPaymentMethod`
- Если `'stars'` → вызывать `initiateStarsPayment()`
- Если другой метод → показать сообщение "Coming soon" или использовать текущий процесс

**Это займет ~1-2 часа разработки.**

---

### Этап 5: Тестирование (вместе)

#### Шаг 5.1: Тестовый платеж
1. Открой checkout страницу в Telegram
2. Выбери тариф
3. Выбери "Telegram Stars" как метод оплаты
4. Нажми "Purchase"
5. Должно открыться окно оплаты Telegram Stars
6. Оплати тестовыми Stars (если есть) или реальными
7. Проверь, что:
   - Платеж обработан
   - Заказ создан в eSIM Go
   - QR код получен и отображен

#### Шаг 5.2: Проверка логов
- Проверить логи сервера на ошибки
- Проверить логи Telegram Bot API
- Убедиться, что webhook получает события

---

## 📋 Чек-лист для тебя

### Сейчас (5-10 минут):
- [ ] Создать бота через @BotFather
- [ ] Сохранить токен бота
- [ ] Отправить мне токен (или добавить в .env на сервере)

### После получения токена (я сделаю):
- [ ] Добавить токен в переменные окружения
- [ ] Настроить webhook для бота
- [ ] Разработать клиентскую часть
- [ ] Протестировать интеграцию

### Перед запуском:
- [ ] Протестировать тестовый платеж
- [ ] Проверить создание заказов
- [ ] Проверить получение QR кодов

---

## 🔧 Технические детали

### Конвертация цен

**Формула расчета Stars:**
```
Stars = (price / (1 - margin) / (1 - telegram_fee) / stars_rate)
```

**Параметры:**
- `price` - **себестоимость** тарифа в USD (cost, не финальная цена для пользователя!)
  - Например, для `esim_1GB_7D_AE_V2` себестоимость = $2.26
  - ⚠️ **Важно**: Используй себестоимость, а не цену с маржой!
- `margin` - наша маржа в долях (0.29 = 29%)
- `telegram_fee` - комиссия Telegram в долях (0.25 = 25%)
- `stars_rate` - курс 1 Star в USD (0.013 = $0.013)

**Пример расчета:**
Для тарифа `esim_1GB_7D_AE_V2` с себестоимостью $2.26:
```
Stars = (2.26 / (1 - 0.29) / (1 - 0.25) / 0.013) ≈ 326 Stars
```

**Переменные окружения:**
- `STARS_RATE=0.013` - курс 1 Star = $0.013
- `STARS_MARGIN=0.29` - наша маржа 29%
- `STARS_TELEGRAM_FEE=0.25` - комиссия Telegram 25%

**Округление**: Вверх до целого числа Stars  
**Минимум**: 1 Star

### Структура payload инвойса
```json
{
  "pid": "plan_id",
  "pt": "standard|unlimited",
  "bn": "bundle_name",
  "cc": "country_code",
  "cn": "country_name",
  "uid": "telegram_user_id",
  "amt": 1000,
  "cur": "XTR"
}
```

### Процесс оплаты
1. Пользователь выбирает тариф → нажимает "Purchase"
2. Если выбран "Telegram Stars":
   - Клиент вызывает `/api/telegram/stars/create-invoice`
   - Сервер создает инвойс через Telegram Bot API
   - Возвращает ссылку на инвойс
3. Клиент открывает инвойс через `tg.openInvoice(link)`
4. Пользователь оплачивает в Telegram
5. Telegram отправляет webhook на `/api/telegram/stars/webhook`
6. Сервер обрабатывает платеж и создает заказ в eSIM Go
7. Пользователь получает QR код

---

## ⚠️ Важные замечания

1. **Безопасность токена:**
   - Никогда не публикуй токен в открытом доступе
   - Не коммить токен в git
   - Используй переменные окружения

2. **Комиссии Telegram:**
   - Telegram берет комиссию 25% (учитывается в формуле)
   - Комиссии Apple/Google (30%) уже учтены в курсе $0.013
   - Наша маржа 29% также учитывается в формуле
   - Все параметры настраиваются через переменные окружения

3. **Тестирование:**
   - Для тестирования можно использовать тестовые Stars
   - Или реальные Stars на небольшой сумме
   - Проверь все сценарии (успех, отмена, ошибка)

4. **Мониторинг:**
   - Настрой мониторинг логов сервера
   - Отслеживай успешные/неуспешные платежи
   - Проверяй создание заказов в eSIM Go

---

## 🚀 Следующие шаги

1. **Ты делаешь:**
   - Создаешь бота и получаешь токен
   - Отправляешь мне токен

2. **Я делаю:**
   - Добавляю токен в переменные окружения
   - Настраиваю webhook
   - Разрабатываю клиентскую часть
   - Тестирую интеграцию

3. **Вместе:**
   - Тестируем полный цикл оплаты
   - Проверяем все edge cases
   - Запускаем в продакшн

---

## 📞 Вопросы?

Если что-то непонятно или нужна помощь:
- Напиши мне
- Проверь документацию: https://core.telegram.org/bots/payments-stars
- Посмотри примеры в коде: `api/telegram/stars/`

**Готов начать, как только получишь токен бота!** 🚀


## 📊 Текущий статус

### ✅ Что уже готово:
1. **API endpoints созданы:**
   - `/api/telegram/stars/create-invoice` - создание инвойса
   - `/api/telegram/stars/webhook` - обработка платежей
   - Маршруты зарегистрированы в `server.js`

2. **UI для выбора метода оплаты:**
   - Карточка "Payment method" на странице checkout
   - Bottom sheet с выбором методов (Telegram Stars, Bank Cards, Crypto Payments)
   - Сохранение выбора в localStorage

3. **Серверная логика:**
   - Конвертация USD → Stars
   - Создание инвойсов через Telegram Bot API
   - Обработка webhook'ов от Telegram
   - Автоматическое создание заказов в eSIM Go после оплаты

### ❌ Что нужно доделать:
1. **Клиентская часть (checkout.js):**
   - Проверка выбранного метода оплаты
   - Вызов API для создания инвойса
   - Открытие инвойса через `tg.openInvoice()`
   - Обработка результата оплаты

2. **Настройка инфраструктуры:**
   - Получить токен Telegram бота
   - Настроить переменные окружения на сервере
   - Настроить webhook для бота (или использовать long polling)

---

## 🎯 Пошаговый план действий

### Этап 1: Настройка Telegram бота (от тебя)

#### Шаг 1.1: Создать бота (5 минут)
1. Открой [@BotFather](https://t.me/BotFather) в Telegram
2. Отправь команду `/newbot`
3. Следуй инструкциям:
   - Введи имя бота (например: `eSimsData Bot`)
   - Введи username бота (например: `esimsdata_bot`)
4. **Сохрани токен бота** (формат: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)
   - ⚠️ **Важно**: Токен секретный, не публикуй его!

#### Шаг 1.2: Проверить поддержку Stars (2 минуты)
1. В @BotFather отправь `/mybots`
2. Выбери своего бота
3. Проверь, что бот поддерживает последнюю версию Bot API
4. Если нужно - обнови бота через BotFather

#### Шаг 1.3: Предоставить токен (1 минута)
- **Отправь мне токен бота** через безопасный канал (не в публичном чате!)
- Или добавь его в переменные окружения на сервере (см. Этап 2)

---

### Этап 2: Настройка сервера (от тебя + я помогу)

#### Шаг 2.1: Добавить переменные окружения на сервере

**Вариант A: Если у тебя есть доступ к серверу:**
```bash
# Подключись к серверу
ssh root@37.60.228.11

# Отредактируй .env файл (или создай, если нет)
nano /var/www/esimsdata/.env

# Добавь следующие строки:
TELEGRAM_BOT_TOKEN=твой_токен_бота_здесь
STARS_RATE=0.013
STARS_MARGIN=0.29
STARS_TELEGRAM_FEE=0.25
TELEGRAM_WEBHOOK_SECRET=случайная_секретная_строка_для_безопасности
```

**Вариант B: Если нет доступа - отправь мне токен, я добавлю**

#### Шаг 2.2: Перезапустить сервер (я сделаю)
После добавления переменных окружения нужно перезапустить Node.js процесс:
```bash
pm2 restart esimsdata
# или
systemctl restart esimsdata
```

---

### Этап 3: Настройка Webhook (я сделаю после получения токена)

#### Шаг 3.1: Настроить webhook для бота
Есть два варианта:

**Вариант A: Webhook (рекомендуется для продакшн)**
- URL: `https://esimsdata.app/api/telegram/stars/webhook`
- Нужен HTTPS сертификат (у тебя уже есть)
- Telegram будет отправлять события платежей на этот URL

**Вариант B: Long Polling (для тестирования)**
- Не требует настройки webhook
- Бот будет периодически опрашивать Telegram API
- Медленнее, но проще для тестирования

**Я настрою webhook автоматически после получения токена.**

---

### Этап 4: Разработка клиентской части (я сделаю)

#### Шаг 4.1: Модифицировать `checkout.js`
Нужно добавить:
1. Проверку выбранного метода оплаты (`selectedPaymentMethod === 'stars'`)
2. Функцию `initiateStarsPayment()`:
   - Получение данных тарифа
   - Вызов `/api/telegram/stars/create-invoice`
   - Открытие инвойса через `tg.openInvoice()`
3. Обработку результата:
   - Успешная оплата → показать QR код
   - Отмена → вернуться на checkout
   - Ошибка → показать сообщение

#### Шаг 4.2: Обновить `setupPurchaseButton()`
- Проверять `selectedPaymentMethod`
- Если `'stars'` → вызывать `initiateStarsPayment()`
- Если другой метод → показать сообщение "Coming soon" или использовать текущий процесс

**Это займет ~1-2 часа разработки.**

---

### Этап 5: Тестирование (вместе)

#### Шаг 5.1: Тестовый платеж
1. Открой checkout страницу в Telegram
2. Выбери тариф
3. Выбери "Telegram Stars" как метод оплаты
4. Нажми "Purchase"
5. Должно открыться окно оплаты Telegram Stars
6. Оплати тестовыми Stars (если есть) или реальными
7. Проверь, что:
   - Платеж обработан
   - Заказ создан в eSIM Go
   - QR код получен и отображен

#### Шаг 5.2: Проверка логов
- Проверить логи сервера на ошибки
- Проверить логи Telegram Bot API
- Убедиться, что webhook получает события

---

## 📋 Чек-лист для тебя

### Сейчас (5-10 минут):
- [ ] Создать бота через @BotFather
- [ ] Сохранить токен бота
- [ ] Отправить мне токен (или добавить в .env на сервере)

### После получения токена (я сделаю):
- [ ] Добавить токен в переменные окружения
- [ ] Настроить webhook для бота
- [ ] Разработать клиентскую часть
- [ ] Протестировать интеграцию

### Перед запуском:
- [ ] Протестировать тестовый платеж
- [ ] Проверить создание заказов
- [ ] Проверить получение QR кодов

---

## 🔧 Технические детали

### Конвертация цен

**Формула расчета Stars:**
```
Stars = (price / (1 - margin) / (1 - telegram_fee) / stars_rate)
```

**Параметры:**
- `price` - **себестоимость** тарифа в USD (cost, не финальная цена для пользователя!)
  - Например, для `esim_1GB_7D_AE_V2` себестоимость = $2.26
  - ⚠️ **Важно**: Используй себестоимость, а не цену с маржой!
- `margin` - наша маржа в долях (0.29 = 29%)
- `telegram_fee` - комиссия Telegram в долях (0.25 = 25%)
- `stars_rate` - курс 1 Star в USD (0.013 = $0.013)

**Пример расчета:**
Для тарифа `esim_1GB_7D_AE_V2` с себестоимостью $2.26:
```
Stars = (2.26 / (1 - 0.29) / (1 - 0.25) / 0.013) ≈ 326 Stars
```

**Переменные окружения:**
- `STARS_RATE=0.013` - курс 1 Star = $0.013
- `STARS_MARGIN=0.29` - наша маржа 29%
- `STARS_TELEGRAM_FEE=0.25` - комиссия Telegram 25%

**Округление**: Вверх до целого числа Stars  
**Минимум**: 1 Star

### Структура payload инвойса
```json
{
  "pid": "plan_id",
  "pt": "standard|unlimited",
  "bn": "bundle_name",
  "cc": "country_code",
  "cn": "country_name",
  "uid": "telegram_user_id",
  "amt": 1000,
  "cur": "XTR"
}
```

### Процесс оплаты
1. Пользователь выбирает тариф → нажимает "Purchase"
2. Если выбран "Telegram Stars":
   - Клиент вызывает `/api/telegram/stars/create-invoice`
   - Сервер создает инвойс через Telegram Bot API
   - Возвращает ссылку на инвойс
3. Клиент открывает инвойс через `tg.openInvoice(link)`
4. Пользователь оплачивает в Telegram
5. Telegram отправляет webhook на `/api/telegram/stars/webhook`
6. Сервер обрабатывает платеж и создает заказ в eSIM Go
7. Пользователь получает QR код

---

## ⚠️ Важные замечания

1. **Безопасность токена:**
   - Никогда не публикуй токен в открытом доступе
   - Не коммить токен в git
   - Используй переменные окружения

2. **Комиссии Telegram:**
   - Telegram берет комиссию 25% (учитывается в формуле)
   - Комиссии Apple/Google (30%) уже учтены в курсе $0.013
   - Наша маржа 29% также учитывается в формуле
   - Все параметры настраиваются через переменные окружения

3. **Тестирование:**
   - Для тестирования можно использовать тестовые Stars
   - Или реальные Stars на небольшой сумме
   - Проверь все сценарии (успех, отмена, ошибка)

4. **Мониторинг:**
   - Настрой мониторинг логов сервера
   - Отслеживай успешные/неуспешные платежи
   - Проверяй создание заказов в eSIM Go

---

## 🚀 Следующие шаги

1. **Ты делаешь:**
   - Создаешь бота и получаешь токен
   - Отправляешь мне токен

2. **Я делаю:**
   - Добавляю токен в переменные окружения
   - Настраиваю webhook
   - Разрабатываю клиентскую часть
   - Тестирую интеграцию

3. **Вместе:**
   - Тестируем полный цикл оплаты
   - Проверяем все edge cases
   - Запускаем в продакшн

---

## 📞 Вопросы?

Если что-то непонятно или нужна помощь:
- Напиши мне
- Проверь документацию: https://core.telegram.org/bots/payments-stars
- Посмотри примеры в коде: `api/telegram/stars/`

**Готов начать, как только получишь токен бота!** 🚀

