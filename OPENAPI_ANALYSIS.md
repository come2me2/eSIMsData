# 📋 Анализ OpenAPI спецификации eSIM Go

## Как использовать

### Вариант 1: Ручной анализ

1. Скачайте OpenAPI YAML файл с сайта eSIM Go
2. Откройте файл в текстовом редакторе
3. Найдите раздел `paths:` - там перечислены все доступные endpoints
4. Ищите endpoints со словами:
   - `catalogue` / `catalog`
   - `bundle` / `bundles`
   - `product` / `products`
   - `package` / `packages`
   - `available`
   - `plan` / `plans`
   - `tariff` / `tariffs`

### Вариант 2: Использование Postman коллекции

1. Скачайте Postman коллекцию
2. Импортируйте в Postman
3. Просмотрите все доступные запросы
4. Найдите запросы для получения каталога/bundles

### Вариант 3: Онлайн просмотр

1. Загрузите OpenAPI YAML в онлайн редактор:
   - https://editor.swagger.io/
   - https://stoplight.io/open-source/elements
2. Просмотрите все endpoints в удобном интерфейсе

## Что искать в спецификации

### Endpoints для каталога

Ищите GET endpoints, которые:
- Возвращают список доступных тарифов
- Принимают параметр `country` (код страны)
- Возвращают структуру с bundles/packages/products

### Примеры названий endpoints

```
GET /catalogue
GET /catalog
GET /bundles
GET /products
GET /packages
GET /available-bundles
GET /plans
GET /tariffs
GET /esims/{iccid}/bundles (для конкретного eSIM)
```

### Структура ответа

Ищите в спецификации структуру ответа, которая содержит:
- Список тарифов/bundles
- Информацию о стране
- Цены
- Объем данных
- Срок действия

## После анализа

Когда найдете правильный endpoint:

1. **Обновите `api/esimgo/client.js`:**
   - Добавьте правильный endpoint в функцию `getCatalogue()`
   - Обновите структуру данных согласно спецификации

2. **Обновите обработку данных:**
   - Адаптируйте `catalogue-processed.js` под реальную структуру ответа

3. **Протестируйте:**
   - Используйте тестовую страницу для проверки
   - Убедитесь, что данные правильно обрабатываются

## Полезные ссылки

- **Swagger Editor**: https://editor.swagger.io/
- **OpenAPI Specification**: https://swagger.io/specification/
- **Postman**: https://www.postman.com/

