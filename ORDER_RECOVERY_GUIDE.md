# 🔧 Руководство по восстановлению заказов и отправке QR кодов

## Проблема
После оформления заказа Telegram бот не отправляет сообщение с QR-кодом, и статистика использования трафика не обновляется.

## Решение

### 1. ✅ Создан endpoint для обработки callback'ов от eSIM Go
**Endpoint:** `POST /api/esimgo/callback`

Этот endpoint автоматически обновляет статистику использования трафика в заказах при получении callback'ов от eSIM Go.

**Настройка в eSIM Portal:**
1. Зайдите в Account Settings -> API Details
2. Установите Callback URL: `https://your-domain.com/api/esimgo/callback`
3. Выберите Callback Version: V3 (рекомендуется) или V2

### 2. ✅ Скрипт для восстановления конкретного заказа

**Использование:**
```bash
node scripts/restore-order-and-send-qr.js <orderReference> [telegramUserId]
```

**Пример:**
```bash
node scripts/restore-order-and-send-qr.js aa73ec03-4bf2-4753-b6a3-17e0aca54eea 123456789
```

Скрипт:
- Получает данные заказа из eSIM Go
- Получает assignments (ICCID, Matching ID, QR код)
- Сохраняет заказ в базу данных
- Отправляет сообщение с QR-кодом пользователю в Telegram

### 3. ✅ Скрипт для проверки всех заказов без QR кода

**Использование:**
```bash
node scripts/check-pending-orders.js
```

Скрипт:
- Проверяет все заказы без QR-кода
- Получает assignments для завершенных заказов
- Обновляет заказы в базе данных
- Отправляет QR-коды пользователям, которые еще не получили их

**Рекомендуется запускать через cron:**
```bash
# Проверка каждые 5 минут
*/5 * * * * cd /path/to/project && node scripts/check-pending-orders.js >> /var/log/check-pending-orders.log 2>&1
```

### 4. ✅ Обновлена обработка статистики использования трафика

Endpoint `/api/esimgo/bundles` теперь:
- Сначала проверяет данные из заказа (обновленные через callback)
- Если данных нет, делает запрос к eSIM Go API
- Это ускоряет загрузку и снижает нагрузку на API

## Восстановление заказа #aa73ec03-4bf2-4753-b6a3-17e0aca54eea

Для восстановления этого конкретного заказа:

1. **Найдите Telegram User ID пользователя:**
   - Проверьте в админ-панели (`/admin/orders`)
   - Или используйте скрипт без userId (он попытается найти автоматически)

2. **Запустите скрипт восстановления:**
   ```bash
   node scripts/restore-order-and-send-qr.js aa73ec03-4bf2-4753-b6a3-17e0aca54eea
   ```

   Или с указанием User ID:
   ```bash
   node scripts/restore-order-and-send-qr.js aa73ec03-4bf2-4753-b6a3-17e0aca54eea 123456789
   ```

3. **Проверьте результат:**
   - Скрипт выведет информацию о процессе
   - Если QR-код был отправлен, пользователь получит сообщение в Telegram
   - Заказ будет обновлен в базе данных

## Настройка callback URL в eSIM Go

1. Войдите в eSIM Portal
2. Перейдите в Account Settings -> API Details
3. В поле "Callback URL" укажите: `https://your-domain.com/api/esimgo/callback`
4. Выберите Callback Version: V3 (с HMAC валидацией) или V2
5. Сохраните настройки

После настройки eSIM Go будет автоматически отправлять обновления об использовании трафика, и статистика будет обновляться в реальном времени.

## Проверка работы

1. **Проверьте endpoint callback:**
   ```bash
   curl -X POST https://your-domain.com/api/esimgo/callback \
     -H "Content-Type: application/json" \
     -d '{"iccid":"8944123456789012345","alertType":"Utilisation","bundle":{"name":"esim_1GB_7D_TH_V2","initialQuantity":1073741824,"remainingQuantity":536870912}}'
   ```

2. **Проверьте логи:**
   - В логах сервера должны появиться сообщения о получении callback'ов
   - Проверьте, что заказы обновляются в базе данных

3. **Проверьте статистику:**
   - Откройте страницу "Current eSIM" в приложении
   - Статистика должна обновляться автоматически при использовании трафика

## Примечания

- Callback'и от eSIM Go могут приходить с задержкой
- Если callback не настроен, статистика все равно будет обновляться при загрузке страницы (через API запрос)
- Скрипт `check-pending-orders.js` можно запускать вручную или через cron для автоматической проверки
