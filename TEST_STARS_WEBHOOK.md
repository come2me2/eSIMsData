# 🧪 Тестирование автоматического создания заказа через Telegram Stars

## 📋 Варианты тестирования

### Вариант 1: Скрипт для симуляции webhook (рекомендуется)

Создан скрипт `scripts/test-stars-webhook.js` для симуляции webhook запроса от Telegram без реального платежа.

#### Использование:

```bash
# Базовый тест с дефолтными параметрами
node scripts/test-stars-webhook.js

# С указанием пользователя
node scripts/test-stars-webhook.js 2515644

# С полными параметрами
node scripts/test-stars-webhook.js 2515644 esim_1GB_7D_TH_V2 TH
```

#### Что делает скрипт:

1. ✅ Создает mock webhook запрос от Telegram
2. ✅ Симулирует successful_payment событие
3. ✅ Вызывает webhook handler
4. ✅ Проверяет создание заказа в eSIMgo
5. ✅ Проверяет сохранение заказа в `data/orders.json`
6. ✅ Выводит информацию о созданном заказе

#### Требования:

- Переменные окружения в `.env`:
  - `ESIMGO_API_KEY` - ключ API eSIMgo
  - `TELEGRAM_BOT_TOKEN` или `BOT_TOKEN` - токен Telegram бота
  - `TELEGRAM_WEBHOOK_SECRET` (опционально) - секрет для webhook

---

### Вариант 2: Реальный тестовый платеж через Telegram

#### Шаги:

1. Откройте приложение в Telegram Mini App
2. Выберите тариф
3. Выберите "Telegram Stars" как метод оплаты
4. Нажмите "Purchase"
5. Оплатите тестовыми Stars (если доступны) или реальными на минимальную сумму
6. Проверьте:
   - Заказ создан в eSIMgo
   - Заказ сохранен в админке
   - Пользователь получил уведомление

#### Проверка результатов:

1. **В админке:**
   - Откройте `/admin/orders.html`
   - Найдите заказ по Telegram User ID
   - Проверьте наличие всех данных (ICCID, Matching ID, SMDP Address)

2. **В логах сервера:**
   ```bash
   # На сервере Contabo
   pm2 logs esimsdata --lines 50
   ```

3. **В базе данных:**
   ```bash
   # Проверка файла orders.json
   cat data/orders.json | grep -A 20 "telegram_user_id"
   ```

---

### Вариант 3: Ручной вызов API для сохранения заказа

Если нужно протестировать только сохранение заказа (без создания в eSIMgo):

```bash
curl -X POST https://esimsdata.app/api/orders \
  -H "Content-Type: application/json" \
  -d '{
    "telegram_user_id": "2515644",
    "orderReference": "test-order-123",
    "iccid": "8944422711111572932",
    "matchingId": "RFK61-KV1SZ-XCWVM-BVN",
    "smdpAddress": "rsp-3104.idemia.io",
    "bundle_name": "esim_1GB_7D_TH_V2",
    "price": 9.99,
    "currency": "USD",
    "status": "completed",
    "payment_method": "telegram_stars",
    "source": "telegram_mini_app",
    "customer": "2515644",
    "provider_product_id": "esim_1GB_7D_TH_V2"
  }'
```

---

## 🔍 Что проверять при тестировании

### ✅ Успешный сценарий:

1. **Webhook получает запрос:**
   - ✅ `pre_checkout_query` обрабатывается
   - ✅ `successful_payment` обрабатывается

2. **Заказ создается в eSIMgo:**
   - ✅ Order Reference получен
   - ✅ Статус заказа `completed` или `processing`

3. **Assignments получены:**
   - ✅ ICCID присутствует
   - ✅ Matching ID присутствует
   - ✅ SMDP Address присутствует

4. **Заказ сохранен в админке:**
   - ✅ Файл `data/orders.json` обновлен
   - ✅ Заказ виден в `/admin/orders.html`
   - ✅ Все поля заполнены корректно

5. **Пользователь уведомлен:**
   - ✅ Сообщение отправлено в Telegram
   - ✅ Заказ виден в разделе "My eSIMs"

### ❌ Возможные проблемы:

1. **Webhook не получает запрос:**
   - Проверьте настройки webhook в Telegram Bot API
   - Проверьте `TELEGRAM_WEBHOOK_SECRET` в `.env`

2. **Заказ не создается в eSIMgo:**
   - Проверьте `ESIMGO_API_KEY` в `.env`
   - Проверьте доступность API eSIMgo
   - Проверьте логи сервера

3. **Assignments не получены:**
   - Заказ может быть еще в статусе `processing`
   - Подождите несколько минут и проверьте снова

4. **Заказ не сохраняется в админке:**
   - Проверьте права доступа к файлу `data/orders.json`
   - Проверьте логи сервера на ошибки
   - Проверьте формат данных в запросе

---

## 📊 Мониторинг

### Логи сервера:

```bash
# PM2 логи
pm2 logs esimsdata

# Или напрямую
tail -f /var/www/esimsdata/logs/*.log
```

### Проверка заказов:

```bash
# Количество заказов для пользователя
cat data/orders.json | jq '.["2515644"] | length'

# Последний заказ
cat data/orders.json | jq '.["2515644"][-1]'
```

---

## 🚀 Быстрый тест

Самый быстрый способ протестировать:

```bash
# На сервере Contabo
cd /var/www/esimsdata
node scripts/test-stars-webhook.js 2515644 esim_1GB_7D_TH_V2 TH
```

Это создаст тестовый заказ без реального платежа и покажет все этапы процесса.




