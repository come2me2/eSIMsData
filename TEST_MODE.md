# 🧪 Режим тестирования (Test Mode)

## ✅ Реализовано

Добавлена возможность тестировать покупки **БЕЗ создания реальных заказов** в eSIM Go.

## 🔧 Как использовать

### Вариант 1: Через URL параметр (рекомендуется)

Добавьте `?test=true` к URL checkout страницы:

```
checkout.html?type=country&code=TH&name=Thailand&plan=plan1&planType=standard&test=true
```

### Вариант 2: Через localStorage

В консоли браузера:
```javascript
localStorage.setItem('esimgo_test_mode', 'true');
// Затем обновите страницу
```

Для отключения:
```javascript
localStorage.removeItem('esimgo_test_mode');
```

### Вариант 3: Через переменную окружения (для всех заказов)

В Vercel Dashboard → Environment Variables:
```
Name: ESIMGO_ORDER_MODE
Value: validate
```

**⚠️ ВНИМАНИЕ:** Это включит тестовый режим для ВСЕХ заказов!

## 🔄 Что происходит в тестовом режиме

### При `test_mode: true`:

1. **Вызывается API с `type: 'validate'`**
   - Проверяется доступность Bundle
   - Показывается цена
   - Проверяется совместимость

2. **НЕ создается реальный заказ**
   - ❌ Средства НЕ списываются
   - ❌ eSIM НЕ создается
   - ❌ Bundle НЕ назначается

3. **Возвращается информация о валидации**
   ```json
   {
     "success": true,
     "test_mode": true,
     "data": {
       "valid": true,
       "total": 9.99,
       "currency": "USD",
       "order": [...],
       "note": "This is a validation request. No real order was created."
     }
   }
   ```

## 📊 Сравнение режимов

| Параметр | Режим тестирования | Реальный заказ |
|----------|-------------------|----------------|
| `type` | `validate` | `transaction` |
| Списание средств | ❌ Нет | ✅ Да |
| Создание eSIM | ❌ Нет | ✅ Да |
| Назначение Bundle | ❌ Нет | ✅ Да |
| Получение QR кода | ❌ Нет | ✅ Да |
| Проверка цены | ✅ Да | ✅ Да |
| Проверка доступности | ✅ Да | ✅ Да |

## 🧪 Примеры использования

### Тест 1: Проверка цены Bundle
```javascript
// В консоли браузера
const response = await fetch('/api/esimgo/order', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
        bundle_name: 'esim_1GB_7D_TH_V2',
        test_mode: true  // ← Тестовый режим
    })
});
const result = await response.json();
console.log('Price:', result.data.total, result.data.currency);
```

### Тест 2: Проверка через checkout
1. Откройте checkout с `?test=true`
2. Нажмите "Purchase"
3. Подтвердите покупку
4. Увидите сообщение о валидации (без создания заказа)

## ⚠️ Важные моменты

1. **По умолчанию:** `test_mode: false` (реальные заказы)
2. **Для тестирования:** Используйте `?test=true` в URL
3. **Для продакшена:** Убедитесь, что `test_mode` не установлен
4. **Переменная окружения:** `ESIMGO_ORDER_MODE=validate` включает тест для всех заказов

## 🔍 Проверка режима

В логах Vercel вы увидите:
```
⚠️ TEST MODE: Using validate instead of transaction (no real order will be created)
```

Или:
```
Order created: { mode: 'transaction', ... }
```

## 📝 Рекомендации

1. **Для разработки:** Всегда используйте `?test=true`
2. **Для тестирования:** Используйте `?test=true` перед продакшеном
3. **Для продакшена:** Убедитесь, что `test_mode` не установлен
4. **Мониторинг:** Проверяйте логи на наличие "TEST MODE"

## 🚀 Быстрый тест

1. Откройте:
   ```
   checkout.html?type=country&code=TH&name=Thailand&plan=plan1&planType=standard&test=true
   ```

2. Нажмите "Purchase"

3. Подтвердите покупку

4. Увидите сообщение о валидации (без создания заказа)

