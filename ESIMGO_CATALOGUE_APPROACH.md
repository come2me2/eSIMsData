# 🔍 Поиск правильного способа получения каталога eSIM Go

## Текущая ситуация

✅ **Что работает:**
- API подключение работает
- Endpoint `/esims` доступен и возвращает структуру `{ esims: [], pageCount, pageSize, rows }`
- Но массив `esims` пустой (это список ваших существующих eSIM, а не каталог)

❌ **Что не работает:**
- Endpoint `/bundles` - "access denied" (403)
- Endpoint `/catalogue` - "Not Found" (404)
- Endpoint `/products` - "access denied" (403)

## Возможные решения

### Вариант 1: Каталог доступен только через портал

Возможно, каталог доступных тарифов можно получить только через веб-интерфейс eSIM Go Portal, а не через API.

**Решение:** Использовать статический каталог или синхронизировать данные вручную.

### Вариант 2: Нужно создать eSIM, затем получить bundles

Возможно, процесс такой:
1. Создать eSIM через `POST /esims` (без bundles)
2. Получить доступные bundles для этого eSIM через `GET /esims/{ICCID}/bundles`
3. Выбрать bundle и заказать его

**Решение:** Реализовать двухэтапный процесс.

### Вариант 3: Использовать другой endpoint

Возможно, есть endpoint, который мы еще не пробовали:
- `/catalog` (без 'ue')
- `/available` 
- `/marketplace`
- `/store`

**Решение:** Проверить все возможные варианты через debug endpoint.

### Вариант 4: Каталог в документации API

В вашей документации API v2.4 может быть указан правильный endpoint для получения каталога.

**Решение:** Проверить документацию в портале eSIM Go.

## Рекомендации

1. **Проверьте документацию в портале:**
   - Зайдите в https://portal.esim-go.com/
   - Найдите раздел "API Documentation" или "Developer Docs"
   - Ищите endpoint для получения каталога/списка доступных тарифов

2. **Обратитесь в поддержку:**
   - Спросите: "What is the correct API endpoint to get a list of all available data bundles/packages for purchase?"
   - Или: "How do I get the catalogue of available eSIM plans through the API?"

3. **Попробуйте создать тестовый eSIM:**
   - Используйте `POST /esims` для создания eSIM
   - Затем `GET /esims/{ICCID}/bundles` для получения доступных bundles
   - Это покажет структуру данных bundles

4. **Альтернативный подход:**
   - Если каталог недоступен через API, можно:
     - Использовать статический каталог (обновлять вручную)
     - Синхронизировать данные из портала периодически
     - Использовать другой API провайдера для каталога

## Следующие шаги

1. Проверьте документацию в портале eSIM Go
2. Попробуйте создать тестовый eSIM и получить его bundles
3. Обратитесь в поддержку eSIM Go
4. Если каталог недоступен через API, рассмотрите альтернативные подходы

