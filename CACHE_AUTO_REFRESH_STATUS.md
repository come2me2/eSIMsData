# Статус автоматического обновления серверного кэша

## 📊 Текущее состояние

### ❌ Автоматическое обновление НЕ настроено

**Проверка на сервере Contabo:**
- ✅ Скрипты существуют: `scripts/refresh-cache.sh`, `scripts/refresh-cache.js`
- ✅ API endpoints работают: `/api/cache/refresh`, `/api/cache/prefill`
- ❌ **Cron job НЕ настроен** - автоматическое обновление не выполняется

## 🔧 Как это должно работать

### Архитектура автоматического обновления

```
Cron Job (каждый день в 2:00 UTC)
    ↓
scripts/refresh-cache.sh
    ↓
1. Вызов /api/cache/refresh?secret=...&type=all
   → Очистка старого кэша
    ↓
2. Вызов /api/cache/prefill?secret=...
   → Загрузка свежих данных из eSIM Go API
   → Сохранение в серверный кэш (in-memory)
    ↓
Пользователи видят обновленные данные
```

### Компоненты системы

#### 1. API Endpoints

**`/api/cache/refresh`** (`api/cache/refresh.js`):
- Очищает указанные части кэша
- Параметры: `type` (all, countries, plans), `secret` (для авторизации)
- Используется для очистки перед обновлением

**`/api/cache/prefill`** (`api/cache/prefill.js`):
- Предзаполняет кэш актуальными данными
- Загружает данные из eSIM Go API
- Сохраняет в серверный кэш (in-memory Map)
- Параметры: `secret` (для авторизации)
- Выполняет:
  1. Загрузку списка стран
  2. Загрузку Global планов
  3. Загрузку Region планов (для всех регионов)
  4. Загрузку Local планов (для всех стран, батчами)

#### 2. Скрипты для cron

**`scripts/refresh-cache.sh`** (bash скрипт):
- Вызывается cron job
- Вызывает `/api/cache/refresh` для очистки
- Вызывает `/api/cache/prefill` для обновления
- Логирует в `/var/www/esimsdata/logs/cache-refresh.log`
- Fallback: если HTTP endpoint недоступен, использует прямой вызов через Node.js

**`scripts/refresh-cache.js`** (Node.js скрипт):
- Альтернативный способ обновления
- Можно запускать вручную: `node scripts/refresh-cache.js`
- Вызывает те же endpoints

**`scripts/prefill-cache.js`** (Node.js скрипт):
- Прямой вызов без HTTP
- Используется для первоначального заполнения кэша
- Можно запускать: `node scripts/prefill-cache.js`

#### 3. Серверный кэш

**`api/_lib/cache.js`**:
- In-memory кэш (Map в Node.js)
- TTL: 24 часа для всех типов данных
- Ключи:
  - `countries:all` - список стран
  - `plans:global` - глобальные планы
  - `plans:region:${region}` - планы региона
  - `plans:local:${countryCode}` - планы страны

## 🚀 Как настроить автоматическое обновление

### Шаг 1: Убедитесь, что скрипт исполняемый

```bash
cd /var/www/esimsdata
chmod +x scripts/refresh-cache.sh
```

### Шаг 2: Проверьте переменную окружения

```bash
# Проверьте, что CACHE_REFRESH_SECRET установлен
grep CACHE_REFRESH_SECRET /var/www/esimsdata/.env
```

### Шаг 3: Настройте cron job

```bash
# Откройте crontab
crontab -e

# Добавьте строку (каждый день в 2:00 UTC)
0 2 * * * cd /var/www/esimsdata && /bin/bash scripts/refresh-cache.sh >> /var/www/esimsdata/logs/cache-refresh.log 2>&1
```

### Шаг 4: Проверьте работу

```bash
# Проверьте список cron jobs
crontab -l

# Запустите скрипт вручную для проверки
cd /var/www/esimsdata
bash scripts/refresh-cache.sh

# Проверьте логи
tail -f /var/www/esimsdata/logs/cache-refresh.log
```

## 📋 Что происходит при обновлении

### Процесс обновления кэша

1. **Очистка кэша** (`/api/cache/refresh`):
   - Удаляет устаревшие записи из in-memory кэша
   - Освобождает память

2. **Загрузка данных** (`/api/cache/prefill`):
   - **Countries**: Загружает список всех стран из eSIM Go API
   - **Global Plans**: Загружает глобальные планы (standard + unlimited)
   - **Region Plans**: Загружает планы для каждого региона (8 регионов)
   - **Local Plans**: Загружает планы для всех стран (батчами по 5)

3. **Применение наценки**:
   - Данные сохраняются в кэш **БЕЗ наценки** (себестоимость)
   - Наценка применяется при каждом запросе из настроек админки

4. **Время выполнения**:
   - ~2-5 минут в зависимости от скорости API
   - Local планы загружаются батчами для оптимизации

## ⚠️ Текущая проблема

**Автоматическое обновление НЕ настроено!**

Это означает:
- ❌ Кэш обновляется только вручную
- ❌ Данные могут устаревать (TTL 24 часа)
- ❌ Пользователи могут видеть устаревшие цены

## ✅ Рекомендация

**Настроить cron job для автоматического обновления каждый день в 2:00 UTC**

Это обеспечит:
- ✅ Автоматическое обновление кэша
- ✅ Актуальные данные для пользователей
- ✅ Минимальная нагрузка на API (обновление в нерабочее время)
