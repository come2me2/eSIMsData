# Статус реализации заказов на Contabo

## 📋 Текущая реализация

### 1. Создание заказов

#### При создании invoice (Telegram Stars)
**Файл:** `api/telegram/stars/create-invoice.js` (строки 404-459)

- ✅ Создается заказ со статусом `on_hold`
- ✅ Временный `orderReference: pending_${invoiceId}`
- ✅ `payment_status: 'pending'`
- ✅ `payment_method: 'telegram_stars'`
- ✅ `payment_session_id: invoiceId`
- ✅ Таймаут устанавливается: `expires_at` = текущее время + 5 минут
- ✅ Создание происходит асинхронно (не блокирует ответ)

**Код:**
```javascript
status: 'on_hold',
orderReference: `pending_${invoiceId}`,
payment_status: 'pending',
expires_at: new Date(Date.now() + 5 * 60 * 1000).toISOString()
```

#### При сохранении заказа в БД
**Файл:** `api/orders.js` (строки 140-317)

- ✅ Статус по умолчанию: `status = 'completed'` (строка 157)
- ✅ Но переопределяется при создании через API
- ✅ Определение `payment_confirmed` и `esim_issued` (строки 211-212)
- ✅ Сохранение всех полей статусов (строки 255-263)

---

### 2. Назначение статусов при создании заказа

#### Статус `on_hold`
**Условия:**
- Заказ создан при создании invoice
- Платеж еще не подтвержден
- eSIM еще не выдана

**Поля:**
- `status: 'on_hold'`
- `payment_status: 'pending'`
- `payment_confirmed: false` (по умолчанию)
- `esim_issued: false` (по умолчанию)
- `expires_at: <время создания + таймаут>`

---

### 3. Назначение статусов при оплате

#### Webhook Telegram Stars
**Файл:** `api/telegram/stars/webhook.js` (строки 438-483)

**Логика определения статуса:**
```javascript
let finalStatus = 'on_hold';
if (success && hasEsim) {
    finalStatus = 'completed';
} else if (success && !hasEsim) {
    finalStatus = 'on_hold';
    console.warn('⚠️ Payment confirmed but eSIM not issued yet. Keeping status on_hold.');
}
```

**Условия для `completed`:**
- ✅ Платеж подтвержден (`success === true`)
- ✅ eSIM выдана (`hasEsim === true`)

**Условия для `on_hold`:**
- ✅ Платеж подтвержден, но eSIM еще не выдана

**Обновляемые поля при оплате:**
- `status: finalStatus` (строка 468)
- `payment_status: 'succeeded'` (строка 478)
- `payment_confirmed: true` (строка 479)
- `esim_issued: hasEsim` (строка 480)
- `esim_checked_at: new Date().toISOString()` (строка 481)
- `expires_at: null` (строка 482) - убирается таймаут
- `orderReference` заменяется с `pending_${invoiceId}` на реальный из eSIM Go (строка 450-452)

---

### 4. Таймауты

#### Определение таймаутов
**Файл:** `scripts/check-order-timeouts.js` (строки 19-24)

```javascript
const TIMEOUTS = {
    telegram_stars: 5 * 60 * 1000,      // 5 минут
    stripe: 20 * 60 * 1000,              // 20 минут
    cryptomus: 60 * 60 * 1000           // 60 минут
};
```

#### Логика проверки таймаутов
**Файл:** `scripts/check-order-timeouts.js` (строки 44-150)

**Проверка:**
1. Проходит по всем заказам со статусом `on_hold`
2. Проверяет `expires_at`:
   - Если `expires_at < now()` → устанавливает `status: 'canceled'`
   - Если `expires_at` нет → вычисляет на основе `payment_method` и `createdAt`
3. Обновляет заказ:
   - `status: 'canceled'`
   - `canceled_reason: 'timeout'`
   - `updatedAt: new Date().toISOString()`

#### ✅ РЕШЕНО: Автоматический запуск настроен

**Текущее состояние:**
- ✅ Скрипт существует: `scripts/check-order-timeouts.js`
- ✅ Логика проверки реализована
- ✅ Cron job настроен (каждую минуту)
- ✅ Логирование в `/root/logs/order-timeouts.log`

**Проверка:**
```bash
# Cron настроен
crontab -l | grep timeout
# * * * * * cd /var/www/esimsdata && /usr/bin/node scripts/check-order-timeouts.js >> /root/logs/order-timeouts.log 2>&1

# Проверка логов
tail -f /root/logs/order-timeouts.log
```

---

## ✅ Что исправлено

### 1. Автоматический запуск скрипта таймаутов настроен

**Реализовано:**
- ✅ Cron job настроен и работает
- ✅ Запуск каждую минуту: `* * * * *`
- ✅ Логирование в `/root/logs/order-timeouts.log`
- ✅ Полный путь к node: `/usr/bin/node`
- ✅ Рабочая директория: `/var/www/esimsdata`

**Команда cron:**
```bash
* * * * * cd /var/www/esimsdata && /usr/bin/node scripts/check-order-timeouts.js >> /root/logs/order-timeouts.log 2>&1
```

**Проверка работы:**
```bash
# Просмотр логов
tail -f /root/logs/order-timeouts.log

# Проверка crontab
crontab -l

# Статус cron service
systemctl status cron
```

---

## 📊 Сводка статусов

| Статус | Когда устанавливается | Условия |
|--------|----------------------|---------|
| `on_hold` | При создании invoice | Платеж не подтвержден |
| `on_hold` | При оплате без eSIM | Платеж подтвержден, но eSIM не выдана |
| `completed` | При оплате с eSIM | Платеж подтвержден И eSIM выдана |
| `canceled` | При таймауте | `expires_at < now()` |
| `failed` | При ошибке | (не реализовано полностью) |

---

## ✅ Что работает

1. ✅ Создание заказа `on_hold` при создании invoice
2. ✅ Установка таймаута `expires_at`
3. ✅ Обновление статуса в webhook при оплате
4. ✅ Проверка наличия eSIM перед установкой `completed`
5. ✅ Скрипт проверки таймаутов существует и работает

## ❌ Что не работает

1. ❌ Статус `failed` не реализован полностью

---

## 🎯 Рекомендации

1. ✅ **Выполнено:** Настроен cron job для автоматической проверки таймаутов
2. ✅ **Выполнено:** Добавлено логирование результатов проверки таймаутов
3. **Желательно:** Реализовать статус `failed` для ошибок платежа/заказа
