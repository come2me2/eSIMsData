# Исправление производительности админ-панели

## Проблема
В админ-панели страницы загружались очень медленно. В логах браузера появлялось предупреждение:
```
cdn.tailwindcss.com should not be used in production
```

## Причина
Использовалась CDN версия Tailwind CSS, которая компилирует стили прямо в браузере во время загрузки страницы. Это крайне медленно, особенно на страницах с большим количеством элементов (например, таблицы заказов).

## Решение
✅ Установлен Tailwind CSS v3 как dev-зависимость  
✅ Создана конфигурация для production-сборки  
✅ Предварительно скомпилирован минифицированный CSS файл (~13KB)  
✅ Обновлены все HTML файлы админки (6 файлов)  

## Результат
- ⚡ **Мгновенная загрузка** страниц вместо долгой компиляции
- 📦 **Минимальный размер** CSS файла (13KB вместо ~300KB CDN скрипта)
- 🚀 **Нет runtime компиляции** в браузере
- ✅ **Нет предупреждений** в консоли

## Обновленные файлы
- `admin/index.html`
- `admin/orders.html`
- `admin/users.html`
- `admin/payments.html`
- `admin/settings.html`
- `admin/login.html`

## Для разработчиков
При добавлении новых Tailwind классов запустите:
```bash
npm run build:css
```

Подробнее см. `admin/README.md`

