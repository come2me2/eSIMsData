# Исправление отображения меню в админке

## Проблема
На некоторых размерах экрана меню в админке не отображалось из-за конфликта CSS правил.

## Решение

### 1. Добавлены явные правила для десктопа (>1024px)
```css
@media (min-width: 1025px) {
    /* Меню всегда видимо */
    aside#sidebar {
        transform: translateX(0) !important;
        display: block !important;
    }
    
    /* Кнопка гамбургера скрыта */
    .mobile-menu-btn {
        display: none !important;
    }
    
    /* Main контент с отступом */
    main {
        margin-left: 16rem !important;
    }
}
```

### 2. Улучшены правила для мобильных устройств (≤1024px)
- Боковое меню скрыто по умолчанию
- Показывается кнопка гамбургера
- Main контент без левого отступа

### 3. Обновлен JavaScript
- Добавлена функция `checkScreenSize()` для проверки размера экрана
- При изменении размера окна автоматически применяется правильное состояние
- Меню закрывается только на мобильных устройствах

## Размеры экранов

| Размер экрана | Поведение меню | Кнопка меню |
|---------------|----------------|-------------|
| > 1024px (Desktop) | Всегда видимо слева | Скрыта |
| ≤ 1024px (Tablet/Mobile) | Выдвигается по клику | Видима |

## Тестирование

Проверьте работу на следующих разрешениях:
- 🖥️ Desktop: 1920x1080, 1366x768, 1280x720
- 📱 Tablet: 1024x768, 768x1024
- 📱 Mobile: 414x896, 375x667, 360x640

## Файлы, которые были изменены
1. `/admin/css/admin.css` - добавлены медиа-запросы и правила для десктопа
2. `/admin/js/mobile-menu.js` - добавлена функция checkScreenSize()

