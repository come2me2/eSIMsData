# 🧪 Инструкция по проверке авторизации Telegram

## ✅ Что уже сделано:

1. ✅ Telegram SDK подключен (`telegram-web-app.js`)
2. ✅ Утилита авторизации подключена (`telegram-auth.js`)
3. ✅ Создана тестовая страница (`test-auth.html`)
4. ✅ Добавлен визуальный индикатор на главной странице

## 🚀 Как проверить авторизацию:

### Вариант 1: Через тестовую страницу (рекомендуется)

1. **Настройте Web App в @BotFather:**
   - Откройте [@BotFather](https://t.me/BotFather)
   - Отправьте `/newapp`
   - Выберите вашего бота
   - Укажите **Web App URL**: `https://your-project.vercel.app/test-auth.html`
   - Или для локального тестирования используйте ngrok

2. **Откройте бота в Telegram:**
   - Найдите вашего бота в Telegram
   - Нажмите `/start` или кнопку меню
   - Откройте Web App

3. **Проверьте результаты:**
   - Должен быть зелёный статус "✅ Авторизация успешна!"
   - Должен отображаться ваш Telegram ID
   - Должны быть видны все данные пользователя

### Вариант 2: Через главную страницу

1. Настройте Web App URL в @BotFather на главную страницу: `https://your-project.vercel.app`
2. Откройте бота в Telegram
3. В правом верхнем углу должен появиться **зелёный индикатор** (если авторизован)
4. Откройте консоль браузера (F12) и проверьте:
   ```javascript
   console.log(window.telegramAuth.isAuthenticated()); // должно быть true
   console.log(window.telegramAuth.getUserData()); // должен вернуть объект
   ```

## 🔍 Что проверять:

### ✅ Успешная авторизация:
- ✅ Статус: "Авторизация успешна!"
- ✅ Telegram ID отображается (число, например: `123456789`)
- ✅ Имя пользователя отображается
- ✅ Индикатор на главной странице зелёный

### ❌ Проблемы:

**Если статус "Telegram SDK не загружен":**
- Откройте приложение через Telegram (не в обычном браузере)
- Проверьте, что Web App URL правильно настроен в @BotFather

**Если статус "Пользователь не авторизован":**
- Убедитесь, что открываете через Telegram WebApp
- Проверьте консоль браузера на ошибки
- Проверьте, что `telegram-auth.js` подключен в HTML

## 📝 Проверка в консоли браузера:

Откройте DevTools (F12) и выполните:

```javascript
// Проверка SDK
console.log('SDK:', window.Telegram?.WebApp ? '✅' : '❌');

// Проверка авторизации
console.log('Auth:', window.telegramAuth ? '✅' : '❌');
console.log('Authenticated:', window.telegramAuth?.isAuthenticated());

// Данные пользователя
console.log('User ID:', window.telegramAuth?.getUserId());
console.log('User Name:', window.telegramAuth?.getUserName());
console.log('Full Data:', window.telegramAuth?.getUserData());
```

## 🎯 Ожидаемый результат:

После успешной авторизации вы должны увидеть:

```javascript
{
  id: 123456789,           // Ваш Telegram ID
  name: "Иван Иванов",     // Полное имя
  first_name: "Иван",      // Имя
  last_name: "Иванов",    // Фамилия
  username: "ivanov",      // Username (если есть)
  language: "ru",          // Язык
  is_authenticated: true   // Статус авторизации
}
```

## 🆘 Решение проблем:

### Проблема: Не видно индикатора на главной странице
**Решение:** Обновите страницу или проверьте, что `app.js` загружается после `telegram-auth.js`

### Проблема: `window.telegramAuth is undefined`
**Решение:** 
- Проверьте порядок подключения скриптов в HTML
- `telegram-auth.js` должен быть подключен после `telegram-web-app.js`

### Проблема: Авторизация не работает
**Решение:**
- Убедитесь, что открываете через Telegram (не в браузере)
- Проверьте Web App URL в @BotFather
- Проверьте, что проект задеплоен на Vercel

## ✅ Готово!

Если всё работает:
- ✅ Telegram ID получается
- ✅ Данные пользователя доступны
- ✅ Можно использовать `window.telegramAuth.getUserId()` в коде

**Следующий шаг:** Настроить сохранение заказов (см. `STORAGE_OPTIONS.md`)

