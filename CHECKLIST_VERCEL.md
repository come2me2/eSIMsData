# ✅ Чек-лист подключения Telegram SDK и авторизации (Vercel)

## 🎯 Супер-короткий чек-лист для продакшн на Vercel

### 1. ✅ Проверить подключение Telegram SDK в проекте

- [ ] Убедиться, что во всех HTML файлах подключен Telegram SDK:
  ```html
  <script src="https://telegram.org/js/telegram-web-app.js"></script>
  ```
- [ ] Убедиться, что `telegram-auth.js` подключен **ПЕРЕД** другими скриптами:
  ```html
  <script src="telegram-auth.js"></script>
  ```
- [ ] Проверить, что скрипт подключен во всех нужных страницах (уже сделано ✅)

### 2. 🤖 Создать бота в Telegram через @BotFather

- [ ] Открыть [@BotFather](https://t.me/BotFather) в Telegram
- [ ] Отправить команду `/newbot`
- [ ] Ввести имя бота (например: `eSimsData Bot`)
- [ ] Ввести username бота (например: `esimsdata_bot`)
- [ ] **Сохранить токен бота** (формат: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 3. 🌐 Получить URL проекта на Vercel

- [ ] Зайти в [Vercel Dashboard](https://vercel.com/dashboard)
- [ ] Найти ваш проект `eSim`
- [ ] Скопировать **Production URL** (например: `https://your-project.vercel.app`)
- [ ] Или использовать кастомный домен, если настроен

### 4. 📱 Настроить Web App в @BotFather

- [ ] В @BotFather отправить `/newapp`
- [ ] Выбрать созданного бота
- [ ] Заполнить форму:
  - **Title**: `eSimsData`
  - **Description**: `Global eSIM for your Travel`
  - **Photo**: загрузить иконку (опционально)
  - **Web App URL**: вставить URL из Vercel (например: `https://your-project.vercel.app`)

### 5. 🧪 Протестировать авторизацию

- [ ] Открыть вашего бота в Telegram
- [ ] Нажать `/start` или кнопку меню
- [ ] Открыть Web App (кнопка "Open" / "Открыть")
- [ ] Открыть DevTools в браузере (F12 → Console)
- [ ] Проверить в консоли:
  ```javascript
  console.log(window.telegramAuth.isAuthenticated()); // должно быть true
  console.log(window.telegramAuth.getUserData()); // должен вернуть объект с данными
  ```

### 6. ✅ Проверить использование Telegram ID в коде

- [ ] Убедиться, что в `checkout.js` сохраняется `telegram_user_id`:
  ```javascript
  telegram_user_id: window.telegramAuth.getUserId()
  ```
- [ ] Проверить, что в `app.js`, `account.js`, `my-esims.js` используется авторизация
- [ ] Протестировать полный флоу: выбор плана → оформление заказа → проверка данных

### 7. 🔒 Настроить переменные окружения (если нужен бэкенд)

Если у вас есть API endpoints для валидации данных:

- [ ] Зайти в Vercel Dashboard → ваш проект → Settings → Environment Variables
- [ ] Добавить переменную (если нужна):
  - `TELEGRAM_BOT_TOKEN` = токен бота (для серверной валидации)
- [ ] Передеплоить проект (Vercel сделает это автоматически при коммите)

**Примечание:** Для фронтенда токен бота не нужен, так как Telegram SDK работает напрямую.

### 8. 💾 Настроить хранение заказов (ВАЖНО!)

**⚠️ Сейчас заказы не сохраняются!** Нужно настроить базу данных.

- [ ] Выбрать вариант хранения (см. `STORAGE_OPTIONS.md`):
  - [ ] Supabase (рекомендуется) — бесплатно, SQL, просто
  - [ ] Firebase — бесплатно, NoSQL
  - [ ] MongoDB Atlas — бесплатно, NoSQL
  - [ ] Другое

- [ ] Создать аккаунт и настроить БД
- [ ] Создать таблицу/коллекцию `orders` с полями:
  - `telegram_user_id`, `telegram_username`, `user_name`
  - `type`, `name`, `code`, `plan_id`, `plan_type`, `price`
  - `created_at`, `status`

- [ ] Создать API endpoint: `api/orders.js` (Vercel Serverless Function)
- [ ] Добавить переменные окружения в Vercel (URL БД, ключи)
- [ ] Раскомментировать код в `checkout.js` для отправки заказов
- [ ] Обновить `my-esims.js` для загрузки заказов с сервера

**📖 Подробности:** См. `STORAGE_OPTIONS.md`

### 9. 🚀 Финальная проверка

- [ ] Открыть бота → Web App
- [ ] Проверить, что авторизация работает
- [ ] Создать тестовый заказ
- [ ] Убедиться, что заказ сохраняется в БД
- [ ] Проверить загрузку заказов в "My eSIMs"
- [ ] Проверить работу на разных устройствах (iOS/Android)

---

## 📝 Быстрая проверка готовности

Выполните в консоли браузера (в Web App):

```javascript
// Проверка SDK
console.log('Telegram SDK:', window.Telegram?.WebApp ? '✅' : '❌');

// Проверка авторизации
console.log('Authenticated:', window.telegramAuth?.isAuthenticated() ? '✅' : '❌');

// Проверка данных пользователя
console.log('User Data:', window.telegramAuth?.getUserData());
```

---

## 🆘 Решение проблем

### Проблема: `window.telegramAuth is undefined`
**Решение:** 
- Проверьте, что `telegram-auth.js` подключен в HTML
- Проверьте порядок подключения скриптов (telegram-auth.js должен быть после telegram-web-app.js)

### Проблема: `isAuthenticated()` возвращает `false`
**Решение:**
- Откройте приложение через Telegram (не в обычном браузере)
- Проверьте, что Web App URL в @BotFather совпадает с URL на Vercel
- Проверьте консоль на ошибки

### Проблема: Web App не открывается в Telegram
**Решение:**
- Проверьте, что URL в @BotFather начинается с `https://`
- Убедитесь, что проект задеплоен на Vercel
- Проверьте, что домен доступен (откройте в браузере)

---

## ✅ Готово!

После выполнения всех пунктов:
- ✅ Telegram SDK подключен
- ✅ Авторизация работает
- ✅ Данные пользователя доступны в коде
- ✅ Заказы сохраняются в базе данных
- ✅ Заказы загружаются в "My eSIMs"
- ✅ Проект готов к использованию

**Удачи! 🚀**

---

## 📚 Дополнительные документы

- **Варианты хранения заказов:** `STORAGE_OPTIONS.md`
- **Полная инструкция по Telegram:** `README_TELEGRAM_SETUP.md`
- **Интеграция:** `INTEGRATION_GUIDE.md`

